# BACKLOG — finite implementation scope (source of truth for the architect agent)

This file is the **authoritative, finite checklist** the autonomous workflow's
architect agent works from. It is derived from the design handoff in `README.md`
and the `MotoTracker*.dc.html` prototypes. The architect must:

1. Read this file every iteration.
2. **Verify each `⬜` item against the actual code** (it may already be done).
3. Tick it `✅` when fully satisfied (feature + KDoc + unit tests that build & pass
   `./gradlew testDebugUnitTest` + clean `assembleDebug` + clean `lintDebug`),
   updating this file.
   - **EXCEPTION — pure on-device / UI behavior** (Compose rendering on screen,
     live GPS, accelerometer lean angle, map tiles, BLE "waves", foreground-service
     recording, Android Auto head-unit, real weather/HTTP round-trips): the loop
     CANNOT verify these (the tester is headless — no device/emulator/GPS). Once the
     code + unit-tested seams (ViewModel logic, mappers, parsers, repositories with
     injected fakes) are done, mark them **`🔬`** ("code-complete, awaiting human
     on-device confirmation"), NOT `✅`. Only claim the seam is wired and unit-tested.
4. Pick the **next `⬜`** item as the iteration's task. `🔬` items are NOT re-picked.
5. When **every item in A, B and D is `✅` or `🔬`**, emit `all_implemented` and STOP.

Status legend: `⬜` todo · `✅` done & verified · `🔬` code-complete, needs human on-device check.

**Do NOT invent new scope.** Speculative refactors, extra "nice to have" tests, or
cosmetic polish are NOT tasks. If only "Out of scope" (section C) remains, the
project is complete → `all_implemented`.

**Ordering rule:** items in section A are foundational — do them top-to-bottom.
Nothing in B can be built or tested before **A1 (project scaffold)** is `✅`.

## Build environment (this machine — respect it)

Android Studio + SDK are already installed. To keep the first build offline-safe
(the SDK has **no `cmdline-tools`/sdkmanager**, so Gradle cannot auto-download or
license new SDK packages), the scaffold MUST target what is installed:
- **`compileSdk = 36`** and **build-tools `36.0.0`** — the ONLY installed platform is
  `android-36` (build-tools 35.0.0 & 36.0.0). Do NOT pick compileSdk 34/35 (not
  installed → download would fail). `minSdk = 26`, `targetSdk = 36`.
- Use an **AGP version that supports API 36** (AGP 8.9+; matches Studio AI-251) via
  the version catalog, with a compatible Kotlin 2.x + Compose compiler plugin.
- The SDK path is provided in `local.properties` (`sdk.dir`, gitignored) — do not
  hardcode it elsewhere.
- The machine JDK is **21** (system + Studio JBR). Set Java **source/target
  compatibility to 17** (`compileOptions`/`kotlin jvmTarget "17"`); do NOT pin a
  `jvmToolchain(17)` that would force downloading a JDK 17. Building on JDK 21 is fine.

---

## A. Foundation & infrastructure (do in order)

| #  | Item | Status |
|----|------|--------|
| A1 | **Android Studio / Gradle project scaffold**: `settings.gradle.kts`, root + `app/` module `build.gradle.kts`, `gradle/libs.versions.toml` version catalog, Compose + Kotlin, `AndroidManifest.xml`, `MainActivity`, min/target SDK, `.gitignore`, a trivial passing unit test, `./gradlew assembleDebug` + `testDebugUnitTest` green. | ✅ |
| A2 | **Design system / theme**: Compose `Theme`/`Color`/`Type`/`Shape` encoding the README tokens; the three themes (cockpit/grid/light) + user accent color; Barlow / Barlow Semi Condensed / JetBrains Mono bundled as font resources. | ✅ |
| A3 | **Navigation shell**: bottom nav (Record · Routes · Riders · Stats · Settings), route-detail back stack, top app bar + sync chip, chrome hidden on login/detail per spec. | ✅ |
| A4 | **DI + app state container**: dependency injection (Hilt or chosen DI) and the app-level state (`screen`, `authed`, `lang`, `theme`, `accent`, `units`) as unidirectional `StateFlow` UI state. | ✅ |
| A5 | **i18n string resources**: `strings.xml` for all 6 languages (pl/en/de/fr/cs/ru), copied from the `TRANS`/`T2` dicts in `MotoTracker.dc.html`; runtime language switch; metric↔imperial unit formatting. | ✅ |
| A6 | **Local persistence**: Room schema for routes/bikes/group/waves + settings (DataStore); an outbound **sync queue** table with retry state. Migrations. | ✅ |
| A7 | **GPStrack sync layer**: HTTP client to `http://192.168.1.145/gpstrack` (server address configurable); repository + sync-queue drainer with offline/online + auto-sync toggles. Client behind an interface (unit-testable with fakes). | ✅ |
| A8 | **Diagnostics logging infrastructure**: `RideDebugLogger` interface (`beginRide()` / `log(tag, message)` / `endRide()`) + `@Singleton` `FileRideDebugLogger` writing plain-text, ISO-8601-UTC, one-line-per-event logs to `context.getExternalFilesDir("ride-logs")/ride-<ts>.log`; writes serialized via a `Channel` on `Dispatchers.IO` (never blocks the 1 Hz recording loop); ALL I/O wrapped in try/catch so logging can never crash a ride; a true no-op when disabled (no file created, cheap early return). Add `debugLoggingEnabled: Boolean = false` to the settings model + `SettingsDataStore`/`WritableSettingsSource`, exposed in `SettingsUiState`. Register a `FileProvider` (`<provider>` in `AndroidManifest.xml` + `res/xml/file_paths.xml`) for later sharing (avoids `FileUriExposedException` on API 24+). Logs live in app-specific storage (not world-readable). Unit-test on a temp dir: enabled → file created + line format correct; disabled → no file, no-op. Depends on A6 (persistence). | ✅ |
| A9 | **GPStrack authentication (infra, extends A7)**: add `login(server, email, password)` to the GPStrack client — **POST form-encoded credentials, capture the `Set-Cookie` session**, store it in a persisted cookie jar (DataStore), and attach the session cookie to all subsequent GPStrack requests; expose an auth/session state and handle 401 / expired session (surface re-login). Client stays behind the A7 interface (unit-testable with fakes). Unit-test on a fake HTTP client: form-body encoding, `Set-Cookie` capture, cookie attached on the next request, 401 handling. Depends on A7. | ✅ |
| A10 | **GPS-track correction (server-side OSRM map-matching, infra)**: `GpsCorrectionClient` interface (with a fake for tests) + `OsrmGpsCorrectionClient` calling OSRM `/match/v1/driving/{lon,lat;…}?geometries=geojson&overview=full&tidy=true&gaps=split` (+ `radiuses`/`timestamps`), `lon,lat` formatting, **chunking long rides (~100 pts/request)**, parsing `matchings[].geometry`. **Quality gate (pure, unit-testable):** accept the corrected trace only when `confidence ≥ threshold` AND matched-point fraction ≥ threshold (thresholds configurable); on `NoMatch`/partial/low confidence → **keep the raw trace** and set status accordingly. **Data model:** each route stores `rawTrace` (immutable source of truth, NEVER deleted), a nullable `correctedTrace`, `correctionStatus` (none / queued / low-confidence / done) and `confidence`. **Correction queue** (route id + state + retry) that drains automatically only when the OSRM server is reachable — mirrors the A6/A7 sync-queue; **LAN-only, no port-forward** (home OSRM stays closed to the internet). Configurable OSRM base URL in settings (DataStore; default `http://192.168.1.142:5001`). Unit-test: request building (coords/chunking/params), response parsing (GeoJSON→points), quality-gate verdicts, queue retry — all with a fake HTTP client. Depends on A6, A7. | ✅ |
| A11 | **Screenshot tests (Paparazzi) — headless UI verification**: add the Paparazzi Gradle plugin + JVM screenshot tests rendering the key Compose screens (Login, Record, Routes, Route detail, Riders, Stats, Settings) across the 3 themes and a couple of states (empty vs populated). Baselines committed; `verifyPaparazziDebug` runs on the JVM (no device/emulator) so the loop can finally *see* UI and catch regressions (empty-map placeholder, wrong theme, broken layout) — converts much of the `🔬` burden into loop-verifiable checks. Loop-verifiable. **Implementation note:** used Roborazzi 1.7.0 (via `io.github.takahirom.roborazzi` plugin) instead of Paparazzi — AGP 8.13.2 exceeds Paparazzi's documented compatibility range; Roborazzi has loose AGP coupling and runs under `testDebugUnitTest`. Verify task: `./gradlew verifyRoborazziDebug`. Baselines in `app/src/test/snapshots/`. | ✅ |
| A12 | **CI (GitHub Actions)**: workflow on push / PR to `main` running `./gradlew testDebugUnitTest lintDebug assembleDebug` (plus `verifyPaparazziDebug` once A11 lands), with Gradle + SDK caching. Guards the autonomous loop's direct-to-`main` commits — today nothing checks them, so a hung/broken test can slip in (as happened this cycle). Add a status badge to `README.md`. Tooling / loop-verifiable. | ✅ |

## B. Screens & features (from README §Screens)

| #  | Item | Status |
|----|------|--------|
| B1 | **Login screen**: server address / e-mail / password + "sign in & sync" and "continue without account" (guest mode → local-only). | 🔬 |
| B2 | **Recording screen**: live map with growing track + position pulse, GPS/weather chips, wind rose, compass; speed/time/distance/altitude/fuel/lean tiles; start/pause/resume/finish controls; foreground location service; accelerometer-derived lean; save → route + toast (online/offline). | 🔬 |
| B3 | **Routes list**: summary tiles (count, total km), GPX import, per-route cards (track thumbnail, name, date · bike, sync/queue marker) → detail. | 🔬 |
| B4 | **Route detail**: map with start/end markers; 6 stat tiles; weather card (or "offline"); speed chart + elevation profile; "meetups on route" (BT waves); export/share + send-to-server. | 🔬 |
| B5 | **Riders**: riding group (add by phone), live feed (requires internet), Bluetooth "waves" (works offline). | 🔬 |
| B6 | **Stats**: 4 summary tiles; distance-per-month bar chart; riding-style summary (avg lean, avg speed, total ascent). | 🔬 |
| B7 | **Settings**: account, my motorcycles (add/select/status), appearance & language, server & sync, sync queue, Bluetooth broadcast profile, system & privacy (work-without-internet, GPS road correction, Android Auto), preferences (units, auto-pause, keep screen on). | 🔬 |
| B8 | **Export sheet + toasts**: GPX export, share link, send to GPStrack; confirmation toasts. | 🔬 |
| B9 | **Android Auto view**: glanceable recording screen (Car App Library templates) — big speed, time/distance, lean/altitude, oversized start/pause/stop; night/day themes. | 🔬 |
| B10 | **Diagnostics UI + ride-logging wiring** (needs A8): in Settings → System & privacy add a **"Diagnostyka"** group with (a) a toggle *"Zapisuj logi diagnostyczne podczas jazdy"* bound to the A8 `debugLoggingEnabled` setting (default off), (b) a *"Udostępnij log"* row that shares the latest ride log via `FileProvider` + `Intent.ACTION_SEND`, and (c) a *"Wyczyść logi"* row that deletes all files in `ride-logs`, with a used-space counter. Wire `RideDebugLogger` into `RecordingViewModel`: `beginRide()` on start; log GPS samples, lean, weather calls, lifecycle (pause/resume/finish) and caught errors; `endRide()` on finish. All new user-facing strings in all 6 languages (pl/en/de/fr/cs/ru). Unit-test: toggle persistence (`SettingsViewModel`, fake source), clear-deletes-files, and the share-`Intent` URI builder (thin wrapper). `🔬`: real share sheet + on-device file writing during a live ride. | 🔬 |
| B11 | **Real online map (osmdroid) — replaces the schematic placeholder** (fixes "map doesn't render on the Record screen"). Add the osmdroid dependency; replace `RecordingMapPlaceholder` (a `Canvas` sine-wave stand-in) with a real `MapView` hosted via `AndroidView` with proper lifecycle handling (`onResume`/`onPause`/`onDetach`): a live growing track polyline + position marker on **Record** (B2), and a route polyline + start/end markers on **Route detail** (B4). Keep the `gpsCorrect` seam. Uses the already-declared `INTERNET` permission; raster OSM tiles cached on disk (an offline tile pack remains a future epic — still section C). Unit-test the testable seam: GPS→screen projection / polyline building. `🔬`: actual map rendering + tiles on-device. | 🔬 |
| B12 | **Wire Login to real GPStrack auth** (needs A9): connect the Login screen (B1) to A9 — "sign in & sync" calls `login()`, on success sets `authed = true` and drains the sync queue, on failure shows an inline error; "continue without account" (guest / local-only) stays. The server-address field feeds A7/A9. Unit-test `LoginViewModel` states (idle/loading/success/error) with a fake auth client. All new user-facing strings in all 6 languages (pl/en/de/fr/cs/ru). `🔬`: real sign-in against a live GPStrack server on-device. | 🔬 |
| B13 | **GPS-correction UX on Route detail** (needs A10): a **Raw | Corrected** segmented toggle (Corrected disabled until it exists; default to corrected when present, else raw); a **"Popraw teraz"** button that enqueues/attempts correction on demand (same A10 path — retry a low-confidence result or run once home on WiFi); and **"Usuń poprawioną trasę"** (safe, reversible — re-runnable from raw). The **raw trace is never deletable** (permanent source of truth); removing a whole ride stays in the Routes list (B3). Correction auto-runs on ride finish when the `gpsCorrect` toggle (Settings) is on. Surface correction status/confidence (e.g. "niska pewność"). All new user-facing strings in all 6 languages (pl/en/de/fr/cs/ru). Unit-test the detail VM: view-toggle state, manual-trigger enqueues, delete-corrected clears only the corrected geometry (raw untouched). `🔬`: real OSRM round-trip + on-device rendering. | 🔬 |
| B14 | **Routes search / filter / sort**: on the Routes list add a search field (by name), filters (by bike, by date range) and sort options (date, distance, duration, max speed). The list-transformation logic in the ViewModel is unit-tested; the UI controls are `🔬`. Loop (logic) + `🔬` (UI). | 🔬 |
| B15 | **Records & achievements**: compute personal records (longest ride, fastest avg/max speed, highest total ascent, most km in a month) and simple streaks/badges from stored routes; surface them on the Stats screen. Pure calculation over the route set → fully unit-testable. Loop-verifiable (calc) + `🔬` (presentation). | 🔬 |
| B16 | **Backup / restore local data**: export routes + bikes + settings to a single JSON/zip file via the Storage Access Framework, and import it back (merge or replace, with confirmation). Protects against reinstall / phone change. Serialization + merge logic unit-tested; the SAF picker + real file I/O are `🔬`. Loop (serialization) + `🔬` (I/O). | 🔬 |
| B17 | **Smart default route names + rename**: routes are saved with an empty name today (`RecordingViewModel` `name = ""`) and cannot be renamed. On finish, generate a default name — **offline** → "<part-of-day> przejażdżka" (poranna / popołudniowa / wieczorna / nocna, from the ride's start time); **online** → prepend the ride's area via reverse-geocoding (`android.location.Geocoder`), e.g. an afternoon ride around Szczecin → **"Szczecin – popołudniowa przejażdżka"** (for a multi-area ride use the dominant/most-visited area). Add a **rename** action on Route detail. Localize the fixed parts in all 6 languages. Unit-test the name composer (part-of-day boundaries, area+part-of-day formatting, geocode-result→name, offline fallback); the real `Geocoder` round-trip is `🔬`. Loop (composition logic) + `🔬` (geocoder). | 🔬 |
| B18 | **Settings reorganized into a dedicated tabbed screen**: today Settings is one long scroll mixing §1–§6+ sections. Turn the top-bar gear into a Settings screen with tabs/segments (Account · Motorcycles · Appearance & language · Server & sync · System & diagnostics · Preferences) so related options are grouped, not mixed; reuse the existing section composables under each tab. Tab-selection state unit-tested; layout/navigation `🔬`. **Check against the README design before changing layout.** Loop (tab state) + `🔬` (UI). | 🔬 |
| B19 | **Bike assignment — default bike + change bike on a saved route**: a "current" bike already exists (`currentBikeId`); surface it clearly as the **default** (used for new rides) and let the user **change the bike assigned to a saved route** from Route detail (picker over active bikes). Add `RouteRepository.setBike(routeId, bikeId)` + a route-detail action; the default maps to `setCurrentBikeId`. Unit-test the repo/VM; pickers are `🔬`. Loop (logic) + `🔬` (UI). | 🔬 |
| B20 | **Persist & resume recording after process death.** The foreground `RecordingService` returns `START_STICKY` (system relaunches it) but there is NO session persistence, so a low-memory / thermal kill loses the in-progress ride (accumulated track, start time, running metrics, bike). Persist the active recording session durably while recording (periodic snapshot to DataStore/Room), and on app/service restart detect an unfinished session and **offer to resume (or auto-resume)** — restoring the track and continuing. Unit-test the snapshot/restore serialization and the "unfinished-session-detected → resume state" logic; the actual kill/relaunch is `🔬`. Complements A8 diagnostics + the foreground service. | 🔬 |
| B21 | **Real Bluetooth "waves" — connectionless BLE advertise + scan** (makes B5 real; today the Wave layer is data-only, no radio). Broadcast a compact payload (rider nick + bike + short ID, ~≤26 bytes) in a BLE **advertisement** under the app's service UUID, and **scan** (low-latency while riding) for that UUID to detect nearby riders — **no GATT connection/pairing** (detection must land in the ~1 s pass window at road speed; a connection would be too slow/unreliable). Run advertise+scan inside the recording foreground service (Android throttles background BLE). Uses the `BLUETOOTH_SCAN`/`BLUETOOTH_ADVERTISE` perms (declared under D2) requested at runtime via D2's permission layer; degrade gracefully with a message if the chipset can't advertise+scan concurrently. Replace the static-seed Wave source with a `BleWaveSource` behind an interface (fake for tests). **Testable seams (unit):** advertisement payload encode/decode (round-trip rider→bytes→rider, truncation, malformed-packet handling), `ScanResult`→`Wave` mapping, and dedup/debounce of repeated advertisements. The actual radio (advertising, scanning, two-device discovery) is `🔬` (needs 2 real devices). Depends on D2 (BT runtime perms) + the foreground service. | 🔬 |
| B22 | **Remember login/guest choice — don't show Login on every launch.** Today `AppUiState` defaults to `screen=LOGIN, authed=false` and is in-memory (D4 debt), so Login appears on EVERY app start. Persist the onboarding/auth decision in DataStore (e.g. `authState = NONE | GUEST | AUTHED`): on startup pick the start destination from it — **AUTHED** → enter the app and restore the A9 session (on 401/expired → fall back to Login/guest with a message); **GUEST** → enter the app directly (local-only), skip Login; **NONE** (first run) → show Login. Login re-appears only on first run, explicit sign-out, or a "sign in" action from Settings. Unit-test the start-destination decision from persisted state + the sign-out reset; screens are `🔬`. Completes the auth part of D4's persistence debt; leans on A9 (session). | 🔬 |

## C. Out of scope (NOT tasks)

- **On-device / fully offline** vector map tiles + an on-device offline map-matching
  engine (Valhalla/GraphHopper on downloaded OSM) — later epic. (Online map = **B11**
  via osmdroid; server-side GPS correction / `gpsCorrect` = **A10 + B13** via the home
  OSRM. Only the on-device offline engine remains out of scope.)
- iOS / cross-platform build.
- Server-side (GPStrack backend) changes — that lives in the MotoTracker `backend/`.
- **Third-party / Google OAuth** sign-in — out of scope. (Real e-mail/password auth
  against the GPStrack server IS now in scope: **A9 + B12**.)

## D. Bugs / priority fixes

| #  | Item | Status |
|----|------|--------|
| D1 | **Crash on "start ride" — missing runtime location permission.** Tapping *Rozpocznij jazdę* on the Recording screen (B2) crashes the app with `java.lang.SecurityException: "gps" location provider requires ACCESS_FINE_LOCATION permission`. Root cause: `FusedLocationClientImpl.locationUpdates` (`app/.../data/location/LocationClient.kt:65`, marked `@SuppressLint("MissingPermission")`) calls `LocationManager.requestLocationUpdates(GPS_PROVIDER, …)` with **no runtime permission grant**; `RecordingViewModel.startLocationUpdates()` (`RecordingViewModel.kt:188`) collects that flow inside a `viewModelScope.launch` where the `SecurityException` is uncaught → app dies. The manifest declares `ACCESS_FINE_LOCATION`/`ACCESS_COARSE_LOCATION` but **no runtime-permission request flow exists anywhere** (the only `rememberLauncherForActivityResult` is GPX import in `RoutesScreen`). **Repro:** Huawei P20 Lite (ANE-LX1, Android 9 / API 28), fresh install → tap start ride. Did NOT reproduce on the emulator (permission pre-granted / step skipped). **Fix direction:** request `ACCESS_FINE_LOCATION` (and `POST_NOTIFICATIONS` on API 33+) before recording — gate `startLocationUpdates()` behind a granted permission via a Compose permission launcher (`rememberLauncherForActivityResult(ActivityResultContracts.RequestPermission())` or Accompanist Permissions), show a rationale/denied state on the Recording screen, AND defensively wrap the `callbackFlow` body in a `try/catch (SecurityException)` that `close(e)`s the flow so a revoked/denied grant degrades gracefully instead of crashing. Add a unit test for the SecurityException-→-flow-closes seam. | 🔬 |
| D2 | **No runtime-permission handling anywhere (umbrella; D1 is its crash symptom).** The app never asks the user for any runtime permission — it only *declares* them in the manifest. On API 23+ declared ≠ granted, so every permission-gated feature silently fails or crashes until the user grants manually in system Settings. Concrete gaps: (a) **`ACCESS_FINE_LOCATION`/`ACCESS_COARSE_LOCATION`** — never requested (causes D1 crash on Record). (b) **`POST_NOTIFICATIONS`** (API 33+) — declared but never requested → the foreground-service recording notification is silently suppressed on Android 13+. (c) **Bluetooth for the "waves" feature (B5)** — `BLUETOOTH_SCAN`/`BLUETOOTH_ADVERTISE`/`BLUETOOTH_CONNECT` are **not even declared** in the manifest and not requested (BLE is still a data-only seam today, so this bites once BLE advertise/scan becomes real). **Fix direction:** add a small central permission layer (a reusable Composable/helper) that requests the right permissions at the right entry points — location when entering Record, notifications on first foreground-service use, Bluetooth when opening Waves — each with a rationale + graceful "denied" UI state; declare the missing Bluetooth perms (with `neverForLocation` flag on `BLUETOOTH_SCAN` where applicable). Unit-test the permission-state → action gating; the actual system dialogs are `🔬` (on-device). | 🔬 |
| D3 | **Add-motorcycle has no input form — name/year/plate/status can't be set.** In Settings → My motorcycles, the *Dodaj motocykl* button (`SettingsScreen.kt:125`) calls `viewModel.addBike(name = <"Nowy motocykl">, year = 2024, plate = "")` with **hardcoded default values and no dialog/screen to enter data**. There is also **no edit path**: `SettingsViewModel` exposes only `selectBike` and `addBike` (no `updateBike`/`editBike`), and `BikeRow`'s only gesture is `onSelect` (mark as current) — so a bike's name/year/plate/status can never be entered or changed. The domain model `Bike(name, year, plate, status)` supports all fields; the UI just never collects them. **Repro:** Settings → My motorcycles → Add → a bike named "Nowy motocykl / 2024" appears with no way to rename or edit. **Fix direction:** add an add/edit-bike dialog (or sub-screen) with fields for name, year, plate and an active/sold status toggle (reuse the existing `OutlinedTextField`/`BcTextField` styling); wire `addBike` to the form values and add `updateBike(id, …)` to the VM; open the form both from the Add button and by tapping a `BikeRow` (or an explicit edit affordance). Unit-test add/update/validation in `SettingsViewModel`; the dialog rendering is `🔬` (on-device). | ✅ |
| D4 | **Changing theme / language / accent / units in Settings has no visible effect.** Two root causes. (1) **`AppStateViewModel` is resolved in two different Compose/Hilt scopes:** `MainActivity` obtains it via `hiltViewModel()` at the Activity root (this instance drives `MotoTrackerTheme(theme, accent)`), while `SettingsScreen` — inside the `NavHost` — obtains it via `hiltViewModel()` scoped to the Settings `NavBackStackEntry`. These are **different instances**, so `appStateVm.setTheme/setAccent/setUnits(...)` from Settings mutate a `StateFlow` the theme never observes → nothing re-renders. (2) **Language can't apply either:** `setLanguage` → `LocaleController.applyLanguage` → `AppCompatDelegate.setApplicationLocales(...)`, but `MainActivity` extends `ComponentActivity`, **not `AppCompatActivity`**, and `androidx.appcompat.app.AppLocalesMetadataHolderService` is not registered → the per-app locale is never applied/recreated. **Repro:** Settings → change theme or language → UI unchanged. **Fix direction:** (a) share ONE app-scoped `AppStateViewModel` — hoist the instance from `MainActivity` and pass it (or its state + callbacks) into `MotoApp`/`SettingsScreen`, or resolve it against the Activity `ViewModelStoreOwner`, so Settings drives the same instance the theme observes; (b) make `MainActivity` an `AppCompatActivity` (Compose still works) and register the AppCompat locales metadata service so `setApplicationLocales` takes effect. Related debt: `AppStateViewModel` is still in-memory (its own KDoc says persistence is deferred to A6, but A6 is `✅`) — settings won't survive restart until DataStore-backed; wire that here or track as follow-up. Unit-test: setters update the observed state, and `LocaleController` is invoked with the correct tag. `🔬`: actual on-device theme repaint + locale switch. | 🔬 |
| D5 | **Lock screen orientation during recording (lean-angle correctness)**: the accelerometer-derived lean angle assumes a fixed device orientation, but `MainActivity` has no `android:screenOrientation`, so free rotation invalidates the reading. Lock the Recording flow to a fixed orientation (portrait) — via `android:screenOrientation` on the activity or a per-screen lock while recording — and re-check lean calibration. **Repro:** rotate the phone while recording → lean tile goes wrong. Loop can assert the manifest/config; correct lean behaviour is `🔬` (on-device). | 🔬 |
| D6 | **Compass appears not to update (needs on-device verification)**: reported not working, but tested indoors/stationary. Finding: the compass/heading is driven by **GPS bearing** (`RecordingEngine.headingDeg = sample.bearingDeg` from `LocationClient`), which only updates while **moving** and has no value indoors/without a fix — so a stationary indoor test showing no movement is expected, **likely not a bug**. **Action:** verify on-device while actually riding (heading should track direction of travel). If a compass that works while stationary is wanted, that's a separate enhancement: add a magnetometer / `TYPE_ROTATION_VECTOR` heading source with tilt compensation (unit-test the azimuth math). `🔬` (verify while riding). | 🔬 |
| D7 | **Recording session lost when navigating away — no nav-lock while recording.** During an active recording the user can switch tabs / open Settings; signing in from Settings flips `authed`, which recomputes `MotoApp`'s `startDestination` and resets the NavHost back-stack, and because `RecordingViewModel` is nav-scoped (`hiltViewModel()` per `NavBackStackEntry`), returning to Record recreates the VM → the live recording UI/state is lost (the `START_STICKY` service may keep the GPS running, but the screen can't be returned to). **Repro:** start recording → Settings → sign in → can't get back to the live recording screen. **Fix direction:** lock navigation to the Recording screen while recording is active — disable bottom-nav tabs and block leaving RECORD (or gate behind a confirm) until pause/finish; ensure `authed`/sign-in changes don't reset the nav/recording session mid-ride (hoist recording state so it survives — activity/service-scoped VM, or have the VM read live state from `RecordingService`). Unit-test the nav-enabled gating vs recording phase; on-device nav behaviour is `🔬`. | 🔬 |
| D8 | **"Keep screen on" toggle does nothing — setting persisted but never applied.** `keepScreenOn` is fully wired for storage (AppSettings / DataStore / SettingsViewModel / switch / backup) but **no code applies it to the window** — there is no `window.addFlags(FLAG_KEEP_SCREEN_ON)` or Compose `keepScreenOn` observing the setting, so the display still dims/locks during a ride. (The `addFlags` calls in the code are `FLAG_GRANT_READ_URI_PERMISSION` for file sharing, unrelated.) **Repro:** Settings → enable "keep screen on" → screen still times out while recording. **Fix direction:** observe `settings.keepScreenOn` and add/clear `FLAG_KEEP_SCREEN_ON` on the activity window (ideally scoped to the Recording screen via a Compose `DisposableEffect`). Unit-test the "setting → should-keep-on" decision; the actual screen-stays-on is `🔬`. Same class as D3/D4 (persisted-but-not-applied) — **also audit other settings (auto-pause, units, GPS-correct) for the same gap.** **Audit result:** `gpsCorrect` → wired via `uiState.gpsOnRoad` ✅. `units` → forwarded to carBridge ✅. `autoPause` → persisted/shown in settings UI but not consumed by RecordingEngine or VM (no auto-pause logic exists yet; this is a new feature, not a dead-setting gap). | 🔬 |
| D9 | **Sync/offline chip hardcoded to Offline — always shows OFFLINE.** The top-app-bar sync chip is passed a static `syncState = SyncState.Offline` in `MotoNavHost.kt` (~line 98); it is never derived from real state, so the app always displays OFFLINE regardless of network or the offline toggle. Underlying logic is fine: `offline`/`offlineOnly` default false, `NetworkMonitor.isOnline` works, and `SyncRepositoryImpl` correctly computes `isOnline && !offlineOnly && !offline && autoSync`. **Fix direction:** compute `SyncState` from `NetworkMonitor.isOnline` + `settings.offline/offlineOnly` + sync-queue pending count (Offline when no network or offline mode; `Queued(n)` when pending>0; else `Synced`) and feed it to the chip. Unit-test the state-derivation; the chip render is `🔬`. | ✅ |
| D10 | **Verify/complete runtime permissions for Bluetooth + on-device saving (extends D2).** Internet = normal install-time perm (auto-granted, no action). **Bluetooth** (SCAN/ADVERTISE/CONNECT, API 31+) is runtime: confirm D2's permission layer actually requests it before B21 waves advertise/scan (rationale + denied handling). **Saving on device:** prefer SAF + app-specific storage (no permission on any API) for backup (B16), GPX export, and log sharing (A8/FileProvider); only if a path writes to PUBLIC/shared storage on API ≤28 (P20 = API 28) declare+request `WRITE_EXTERNAL_STORAGE` with `android:maxSdkVersion="28"`. Verify each save path works on API 28 without silently failing for lack of permission. Unit-test the permission gating; on-device grant flow is `🔬`. **Code-complete:** BT perms declared + requested via rememberFeaturePermission; backup/log sharing use SAF/FileProvider; GPX export now wired via SAF CreateDocument in RouteDetailScreen (no WRITE_EXTERNAL_STORAGE on any API). On-device: SAF file-picker + BT system grant dialog remain 🔬. | 🔬 |
| D11 | **App crashes app-wide on long routes — trace stored inline overflows CursorWindow (~2 MB/row).** Confirmed on-device (Huawei P20): the full GPS trace is a JSON blob in the `pathJson` column of the `routes` row; a long ride ("Szczecin – nocna przejażdżka", ~9.9 h, ~35k points, `pathJson` = **1.21 MB**) makes `RouteDao.observeAll()` exceed Android's `CursorWindow` → `SQLiteBlobTooBigException: Row too big to fit into CursorWindow` → **crashes on Routes AND Settings** (any screen reading the routes list). **Long routes are legitimate/expected (long moto rides) — the fix MUST scale to arbitrarily large traces, NOT trim the ride.** **Fix:** (1) list/summary queries must **never SELECT `pathJson`/`correctedPathJson`** — project a lightweight `RouteSummary` (id, name, date, km, dur, thumbnail, status) via DTO/`@DatabaseView`; (2) store the full trace **out-of-row** so any single read fits — file per route (`ride-traces/<id>.json`) or a `route_trace_chunk` child table (read on demand, chunked; never a single >2 MB cursor row); load full trace only on Route detail; (3) **downsample only for display** (thumbnail/chart/osmdroid map), preserving raw points. Room migration required. Unit-test: summary query excludes blobs; out-of-row trace round-trip; display downsampler. `🔬`: on-device open of a multi-hour route without crash. **Related:** `autoPause`/stationary detection (D8 audit: unused) would stop overnight parking logging 35k jitter points; altitude `-3m` = stationary GPS noise, not a bug. | ✅ |
| D12 | **Route-detail map stuck at world zoom — track + tiles not visible.** On-device (Huawei P20) the osmdroid map on Route detail showed only low-zoom world tiles with the GPS track as a sub-pixel dot, for every route. Root cause: `OsmTrackMap` called `MapView.zoomToBoundingBox()` / `controller.setZoom()` inside the `AndroidView` `update` block on the first pass, when the MapView has not been laid out yet (width == height == 0); osmdroid's zoom math is a no-op with no view dimensions, so the camera stayed at the world-level default → track invisible and only zoom-1 tiles requested (tiles/UA/network were all fine). **Fix:** defer camera positioning (`zoomToBoundingBox` / `setCenter`+`setZoom`) until the view has real dimensions — apply immediately when `width>0 && height>0`, otherwise via `MapView.addOnFirstLayoutListener`. Overlays (polyline, markers) still added immediately. Verified on-device: map now fits the track bounds, region MAPNIK tiles load, polyline + start/end markers render. Surfaced while verifying D11. | ✅ |

## E. Enhancements (on-device review 2026-07-14)

| #  | Item | Status |
|----|------|--------|
| E1 | **Route detail: show which bike + make "change bike" discoverable.** The change-bike capability already exists in code (`RouteDetailScreen` has `BikePickerDialog` + `onChangeBike`, VM exposes `bikeName`/`currentBikeId`/`assignableBikes`/`setBike`, verified by tests) but on-device the user can't see the assigned bike or find how to change it. **Fix direction:** surface the bike prominently near the top of the route-detail header — show the bike name (with a "sold" badge when applicable, "—" when none) and an obvious tap/edit affordance that opens the existing `BikePickerDialog`; make it clearly interactive when the garage has ≥1 bike. No new data/VM work, this is a visibility/UX fix. `🔬` on-device. | 🔬 |
| E2 | **Bike detail screen with ridden-routes summary.** There is no bike-detail screen today (only `BikeRow` in Settings → My motorcycles and `BikeFormValidation`). **Add** a bike-detail screen, opened by tapping a bike, showing aggregate stats over all routes assigned to that bike: ride count, total distance, total moving/elapsed time, total fuel + total cost (see E3), longest ride, top speed, and a tappable list of those routes (navigate to route detail). Reuse `PersonalRecordsCalculator`/summary aggregation filtered by `bikeId` (uses the blob-free `RouteSummaryModel`). Unit-test the per-bike aggregation; screen render `🔬`. | 🔬 |
| E3 | **Real fuel model: per-bike tank capacity + fuel price; today fuel is a pure estimate.** The "Paliwo … L szac." figure is `RecordingEngine.fuel = distanceKm × 5.0 L/100km` — a hardcoded distance estimate (`RecordingEngine.kt:18,94`), not measured. **Add** per-bike settings: tank capacity (L), fuel price (per L, with currency), and optionally per-bike consumption (L/100km) to replace the global 5.0 constant. **Route detail:** show fuel amount **and computed cost** (fuel × price), and make both fuel amount and price **editable per route** (persist on the route; cost recomputes). Schema additions on `bikes` (+ maybe `routes.fuelPricePerL`/override) → Room migration. Unit-test cost math + per-bike consumption + edit persistence; UI `🔬`. **Related:** E4 (fill-to-full), E2 (per-bike fuel totals). | ✅ |
| E4 | **Recording screen: "fill to full" quick action + real fuel-used derivation.** Add a tappable icon/button on the Recording screen to log a **refuel-to-full** event (anchors fuel to the bike's tank capacity from E3). Use it to derive real fuel used instead of the flat estimate. **Decided (2026-07-14): simple "last full tank" model** — tapping "fill to full" resets a distance counter; fuel used = per-bike consumption × distance since the last full tank; show remaining fuel/range and warn when the projected range approaches tank capacity. (No multi-event refuel ledger.) Depends on E3. Unit-test the fuel-used/refuel logic; on-device interaction `🔬`. | 🔬 |
| E5 | **Recording screen: move trip timer to bottom + add "moving time" timer; log points only while moving.** Move the total-duration timer (`TimeTile`, currently top in `SpeedAndTimeRow`) to the bottom of the Recording screen, and beside it a **second timer that counts only time in motion** (speed > threshold, e.g. ≥ ~2 km/h to reject GPS noise). Change GPS point logging to **record points only while moving** — this also fixes the D11 root cause of 35k overnight-jitter points ("Szczecin – nocna": 9.9 h elapsed, 3.8 km, avg 0). Interacts with `autoPause` (D8, currently unused). Unit-test moving-time accumulation + the point-gating threshold; layout `🔬`. | 🔬 |
| E6 | **Compass: add analog dial (digital below) and move it next to the speedometer.** `CompassTile` shows only degrees (digital) inside `LeanCompassRow`. **Add** an analog compass — a rotating rose/needle (Canvas) with the numeric heading beneath it — and relocate the compass to sit **next to the speedometer** (top `SpeedAndTimeRow` area). Note D6: heading comes from GPS bearing (updates while moving); for a stationary-usable needle consider a magnetometer/`TYPE_ROTATION_VECTOR` source (optional, separate). Unit-test any azimuth/rotation math; visual `🔬`. | 🔬 |
| E7 | **Lean-angle visualization: live tilt bar (recording) + L/R max gauge (route detail).** Today `LeanTile` shows a rotated bike silhouette + degrees; only a single max `lean` is stored. **(a) Recording:** add a flat horizontal tilt bar under the speedometer+compass showing current lean L↔R with ghost markers at the max-reached left/right, digital value below. **(b) Route detail:** a graphical symmetric gauge of max lean left and max lean right. **Decided (2026-07-14): flat horizontal bar** for both — centered on 0°, fills left/right with the current lean, "ghost" markers at max-reached left/right, numeric value beneath the bar; the detail view shows the same bar frozen at the ride's max L/max R (mirrored L↔R). Requires tracking **max-left and max-right lean separately** during recording (schema + metrics addition, Room migration). Unit-test max-L/max-R accumulation; visuals `🔬`. | 🔬 |
| E8 | **Route detail: full-screen map view.** Add a way to expand the route map to full screen (fullscreen button / tap-to-expand) with the track fitted, and collapse back. Reuse `OsmTrackMap` (the D12 layout-fit fix applies; re-verify `zoomToBoundingBox` fits after the fullscreen container is laid out). Interaction `🔬`. | 🔬 |

## F. Recording-screen fixes & fuel-settings UI (on-device feedback 2026-07-14)

| #  | Item | Status |
|----|------|--------|
| F1 | **Recording screen redesign — restore clipped controls + compact layout (REGRESSION).** After E5/E6/E7 the Recording screen (`RecordingScreen.kt`) is a non-scrolling Column that now **overflows**: the primary **"Rozpocznij trasę"/pause button AND the E4 "fill to full" action are pushed off-screen** (behind the bottom nav) — recording cannot be started. Confirmed on-device (P20): content ends at the two oversized timers (~y1478), bottom nav at y2058, no start button between. **Acceptance:** (a) **restore access to start/pause + fill-to-full** — everything visible without scrolling on a P20-class screen (or make the screen scroll as a fallback); (b) **replace start / pause / fill-to-full text buttons with compact icon buttons** (play / pause / fuel-pump); (c) **timers compact:** put "Czas jazdy" + "Czas w ruchu" on a **single row, much smaller font** (they currently render as two huge tiles whose digits wrap "00:0"→"0:00"); (d) **Speed + Compass tiles: equal height, split 50/50 width** (compass tile is currently taller than speed, uneven); (e) **analog compass dial: add cardinal labels N/E/S/W** on the dial (today only tick marks + a separate "N" text). (f) **Regression guard (headless, REQUIRED):** add a Roborazzi + Robolectric screenshot test of `RecordScreen` in **Idle** at a phone-class size (e.g. 1080×2280) that **(i) asserts the start/pause AND fill-to-full controls are displayed via `assertIsDisplayed()`** — so a clipped/off-screen control FAILS deterministically (pixel-diff alone is insufficient for "is it clipped") — **and (ii) captures a baseline PNG**. This runs in the existing headless `testDebugUnitTest` and would have caught this regression. Also unit-test layout-independent logic (timer formatting). The pixel-perfect look remains `🔬` (on-device). Blocks on-device verification of E4/E5. | ✅ |
| F2 | **Compass + lean must be live BEFORE recording (Idle state).** So the rider can orient/mount the phone before starting. **Compass:** today heading = GPS bearing (`RecordingEngine.headingDeg`), which is 0°/N while stationary (see D6) → add a **magnetometer / `TYPE_ROTATION_VECTOR` heading source** with tilt compensation, active in Idle, feeding the compass tile (unit-test the azimuth math; sensor read is `🔬`). **Lean:** start the accelerometer lean source in Idle too (the `LeanSensorSource` exists and is used during recording) so the Przechył bar/value moves before start. **Acceptance:** on the Idle Recording screen, rotating the phone moves the compass needle and tilting moves the lean bar — no ride required. | 🔬 |
| F3 | **Complete E3 fuel settings UI — bike add/edit form is missing the fuel input fields.** E3 shipped the data model, Room migration, `BikeFormValidation` (`tankCapacityL`/`fuelPricePerL`/`consumptionLper100km`), and strings (`label_tank_capacity`/`label_fuel_price_per_liter`/`label_consumption_l100km`), **but the bike add/edit form in `SettingsScreen.kt` never renders input fields for them** (grep: those labels are used nowhere in `ui/`), so the user cannot set tank capacity / fuel price / consumption — leaving the whole fuel model inert (fuel cost/derivation stays null). **Acceptance:** add the three numeric fields (tank capacity L, fuel price per L, consumption L/100km) to the add/edit-bike form, wired to the existing `BikeFormValidation`/`SettingsViewModel` seam and persisted per bike; validation errors surfaced. Unit-test the form→validation→save wiring; dialog render `🔬`. | 🔬 |

## G. On-device feedback — recording, fuel, routes (2026-07-14, round 2)

| #  | Item | Status |
|----|------|--------|
| G1 | **Remove the map from the Recording screen; keep the GPS-satellite count.** The Recording screen currently embeds an osmdroid map that (offline) is just a placeholder and eats vertical space. **Remove the map widget from the Recording screen entirely.** **Keep** the useful **"GPS · N sat" satellite-count** indicator (today it's the `GpsChip` overlaid on the map, `RecordingScreen.kt:281`) — relocate it to a compact chip/row on the Recording screen. The map stays on Route detail (E8/D12) — this applies ONLY to the Recording screen. Frees vertical space for controls. Unit-test any sat-count formatting; layout is `🔬`. | 🔬 |
| G2 | **Recording controls + fuel readout while recording.** Control set by phase: **Idle → Start**; **Recording → pause · stop · fill-to-full (fuel icon)** — the E4/F1 refuel action must appear as an icon once recording starts, next to pause/stop (today the fuel action isn't surfaced during a ride). Also **display fuel: litres + price** during the ride — surface the active bike's fuel figures (litres used/estimated per E3/E4 + the configured price per litre, and/or running cost). Unit-test control-visibility-per-phase + the fuel readout logic; visuals `🔬`. Refines F1/E4 (fuel model from E3). | 🔬 |

| G3 | **Compass freezes once recording starts (F2 gap).** F2 made the compass live in Idle via the magnetometer / `TYPE_ROTATION_VECTOR` source, but on-device the needle stops updating when recording begins — the heading source likely reverts to GPS bearing (0° when slow/stationary) or the sensor collector is stopped/overridden for the Recording phase. **Fix:** keep the magnetometer/rotation-vector heading feeding the compass across **ALL** phases (Idle · Recording · Paused); never freeze the needle when recording. Unit-test the phase→heading-source selection; on-device movement is `🔬`. | 🔬 |
| G4 | **Delete a route.** No single-route delete is exposed — `RouteRepository` has only `deleteAll` (`RouteDao.delete(entity)` exists but is unused). **Add** `deleteRoute(id)` to the repository + a delete affordance (route-detail overflow menu "Delete", and/or long-press/swipe on the routes list) behind a confirm dialog; navigate back to the list afterwards. Cascade removes trace chunks (FK cascade), sync-queue and correction-queue rows. Unit-test repo delete + cascade + VM wiring; dialog is `🔬`. | ✅ |
| G5 | **Per-route refuel log, itemized in Route detail (REVISES E4).** Supersede E4's simple "last full tank" derivation with a **refuel-event ledger**: each refuel (via the G2 fuel icon during a ride, or added from detail) records an event `{epochMs, litres, pricePerL}`. Tapping the fuel icon opens a quick input **pre-filled with the bike's tank capacity (litres) + configured price/L (E3), both editable**, then saves the event. **Route detail lists every refuel: date, time, litres, price/L** (+ per-fill cost; total fuel + total cost = sum of events). New `refuel_event` table (`routeId` FK cascade, `epochMs`, `litres`, `pricePerL`) + Room migration. Keep "fuel used" derivable, but drive it from logged events rather than the single-reference E4 model. Unit-test ledger CRUD + totals + the input defaults; UI is `🔬`. | 🔬 |

## H. Recording-screen sizing & controls (on-device feedback 2026-07-14, round 3)

| #  | Item | Status |
|----|------|--------|
| H1 | **Recording screen: shrink Speed + Compass and make the layout responsive to screen size.** On-device (P20) the Prędkość + Kompas tiles are oversized (~⅓ of the screen), so on smaller phones the full control column (chips → speed+compass → dystans/wysokość/paliwo → przechył → timers → start/stop controls) won't fit without scrolling. **Make the Recording screen adaptive** — size the tiles/fonts relative to available height (`BoxWithConstraints` / weight-based fills / responsive text sizing) so **everything fits on one screen across phone sizes** without scrolling; noticeably shrink Speed + Compass while keeping them glanceable while riding. Unit-test any pure sizing logic; visual fit is `🔬` (verify on ≥2 screen sizes). | 🔬 |
| H2 | **Fuel/refuel icon missing from the recording controls (G2 gap).** Confirmed on-device: while **Recording**, the control row shows only **Pause + Stop** — the fill-to-full **fuel icon does NOT render**, though G2 specified `pause · stop · fuel`. **Fix:** show the fuel icon in the recording control row so it is `[pause · stop · fuel]` during Recording; tapping it opens the litres+price refuel input (G5) and logs a `refuel_event`. Also **verify the Paused-state controls** — on-device the paused screen shows only the Resume (▶) button; confirm Stop (and fuel) are reachable while paused, not only while actively recording. Unit-test control-set-per-phase (must include fuel for Recording); visuals `🔬`. Refines G2. | 🔬 |

| H3 | **Fuel icon colour by remaining range (percentage-based).** Tint the recording-screen fuel/refuel icon (H2) by remaining fuel as a fraction of the bike's **full range**, where full range = `tankCapacityL / consumptionLper100km × 100` km and remaining is derived from litres used since the last full tank (G5). **Thresholds (decided 2026-07-14): 🔴 red = remaining < 20% of full range OR remaining range < 100 km (the absolute 100 km reserve wins first, protecting small-tank bikes); 🟡 yellow = 20–50%; 🟢 green = > 50%.** Provide a pure, unit-testable colour-selection function (remaining-fraction + remaining-km → colour) handling edge cases: missing fuel config (tank/consumption null) → neutral/grey icon; zero/negative consumption → grey. Unit-test the three bands + the 100 km override + the grey fallbacks; the actual icon tint on screen is `🔬`. Depends on H2 (icon) + E3/G5 (fuel model). | ✅ |

| H4 | **Fuel consumption from real refuel data, fallback to bike setting.** Compute the **actual** consumption (L/100km) from the refuel ledger (G5): between consecutive full-tank refuels, `consumption = litres_refuelled / distance_ridden_between_fills × 100`. Use this real figure everywhere consumption drives a number — fuel used, cost, and the remaining-range / H3 icon colour. **When there isn't enough refuel history** to compute it (fewer than the needed fills), **fall back to the bike's configured `consumptionLper100km`** (E3). Provide a pure, unit-testable calculator (refuel events + distances → L/100km; insufficient data → configured fallback; guard divide-by-zero). Unit-test both the real-data path and the fallback + edge cases. Depends on E3/G5. | ✅ |
| H5 | **Recording top chip row: left-align satellites + show driving range; drop the duplicate compass.** In the G1 chip row (`GpsChip` / `WeatherChip` / `WindRose`), **left-align the GPS satellite-count chip**, and **show the driving range ("zasięg jazdy")** — remaining km from the H3/H4 fuel model — in that row. **When range isn't computable yet** (no tank/consumption/refuel data), show a **"szacowanie zasięgu jazdy" (estimating range)** placeholder instead of a number. **Remove the redundant `WindRose` "second compass"** (the E6 compass tile already shows heading) and the row's **"offline" chip** (sync/offline status is already conveyed by the top-bar queue badge). Unit-test the range formatting + estimating-placeholder logic; layout is `🔬`. Depends on H3/H4. | 🔬 |

## I. Fuel-icon bugs (on-device feedback 2026-07-15, round 4)

| #  | Item | Status |
|----|------|--------|
| I1 | **Tapping the recording fuel icon does NOT open the refuel-input dialog (BUG).** Confirmed on-device: during recording, tapping the fill-to-full fuel icon (H2) does nothing — no dialog to enter **litres + price per litre** appears, so no `refuel_event` can be logged. G5 specified that tapping the icon opens a quick input (pre-filled with the bike's tank capacity litres + configured price/L, both editable) and saves a `refuel_event`. **Fix:** wire the recording-screen fuel icon's `onClick` to open that refuel-input dialog; on confirm, persist a `refuel_event` (G5) for the active route. Ensure the dialog composable exists and is actually reached from the recording icon (verify the seam wasn't left unwired). Unit-test the VM open-dialog/save-refuel seam; dialog render is `🔬`. Depends on G5/H2. | ✅ |
| I2 | **Recording fuel icon stays white/neutral even when fuel data exists (H3/H4 not computing).** On-device: after configuring the bike's fuel settings (F3/E3) and adding refuels to a prior route, the recording fuel icon should be **coloured** by remaining range (H3), using consumption derived from the refuel ledger or the configured fallback (H4) — but it stays the neutral white/grey "no-data" tint. **Fix:** ensure the icon-colour path actually feeds the H3 `FuelRangeIndicator` with a real remaining-range estimate: full range = `tankCapacityL / consumption × 100`; consumption from H4 (refuel ledger → fallback to bike `consumptionLper100km`); remaining = tank minus fuel used since the last full-tank event (which spans rides). Right after a full tank the icon should be **green**. The grey fallback must apply **only** when there is genuinely no tank/consumption data — not when the bike is configured and refuels exist. Unit-test: configured bike (+/− refuels) → non-grey colour with correct band; truly-empty config → grey. Depends on H3/H4/E3/G5/I1. | ✅ |

## J. Help, terms, waves toggle, stop-confirm, resume-route (feedback 2026-07-15, round 5)

| #  | Item | Status |
|----|------|--------|
| J1 | **Help / "Pomoc" screen describing app functionality.** Add a **Settings → Pomoc** entry opening a Help screen that describes the app's features (recording + live tiles, routes list/detail, GPS correction, fuel model + refuels, waves/pomachania, backup, themes/units). All copy as string resources in **all 6 languages** (pl/en/de/fr/cs/ru). (About/version can live on the same screen.) Unit-test navigation/state wiring; content render `🔬`. | ✅ |
| J2 | **First-launch terms/disclaimer to accept.** On first launch (before entering the app), show a **terms screen the user must accept**. Content must state: (a) the author bears **no responsibility** for use of the app; (b) the app **must not be operated while riding**. Copy in **all 6 languages** (pl/en/de/fr/cs/ru). Persist the accepted flag in DataStore; show only until accepted; block app use until accepted (Accept / Decline→exit). Unit-test the "accepted → not shown again" gate + persistence; screen render `🔬`. | 🔬 |
| J3 | **Bluetooth waves (pomachania) enable/disable setting.** Add a Settings toggle (DataStore-backed, like other prefs) to **enable/disable the BLE "waves" feature**; when off, the waves advertise/scan + UI entry points are gated off. Unit-test the setting → feature-gate seam; on-device BLE behaviour `🔬`. | ✅ |
| J4 | **Stop-recording confirmation dialog.** When the user taps **Stop** during recording, show a confirm dialog **"Czy na pewno zakończyć jazdę?"** (are you sure you want to end the ride?) with confirm/cancel; only stop+save on confirm, otherwise keep recording. Copy in all 6 languages. Unit-test the VM confirm-gate (stop only fires after confirm); dialog render `🔬`. | 🔬 |
| J5 | **Resume/continue a saved route.** In each saved route (Route detail), add a **"Continue route"** action that **resumes recording by appending to that existing route** — load its stored trace/metrics as the starting state, continue logging new GPS points onto the same route, and re-save (trace + distance/duration/etc. extended, not a new route). Reassemble the existing trace from chunks (D11 storage), seed the recording session from it, and persist back. Unit-test the resume-seeding + append + re-save logic (repository/engine seam with fakes); on-device continuation `🔬`. Depends on the D11 out-of-row trace storage + recording engine. | 🔬 |

## K. Bike summary, consumption auto-update, recording fit (feedback 2026-07-15, round 6)

| #  | Item | Status |
|----|------|--------|
| K1 | **Bike detail summary shows no data despite assigned routes (E2 aggregation BUG).** On-device the bike-detail summary (longest ride, total fuel, max speed, ride count, etc.) is empty. Verified in the DB: the bike "yamaha" has **4 of 14 routes assigned** (`bikeId` set), so the summary MUST aggregate those 4 — but it renders blank. **Fix:** the E2 `BikeStatsCalculator` / `BikeDetailViewModel` path — ensure routes filtered by `bikeId` actually feed the summary tiles (longest ride, total distance, total time, total fuel + cost, max speed, highest climb, …). Likely the summary reads an empty/unfiltered source or the flow isn't collected. Unit-test aggregation over a fixture of assigned+unassigned routes (only assigned counted, non-empty result); render `🔬`. Fixes E2. | 🔬 |
| K2 | **Per-bike "auto-update consumption from refuels" checkbox.** In the bike add/edit form (F3), add a checkbox that, when enabled, lets the configured `consumptionLper100km` be **automatically updated to the value computed from the refuel ledger (H4)**. When on: after enough refuels, the stored consumption is refreshed with the H4-derived figure (and used everywhere). When off: the manually-entered value is kept. Persist the flag per bike (Room, migration). Unit-test: flag on + sufficient refuels → consumption updates to computed; flag off → stays manual. Depends on H4/G5/E3. | 🔬 |
| K3 | **Shrink the Start-recording button area so the whole Recording screen fits with no scrolling.** H1 added responsive breakpoints but on-device the Recording (Idle) screen still needs scrolling to reach Start — the big round Start button + its hint eat too much height. **Fix:** shrink the start-button block — **remove the "Pauza także przyciskiem na kierownicy" hint text entirely** (user request; also frees height), make the Start button smaller, and tighten spacing so the entire Idle screen (chips → speed+compass → dystans/wysokość/paliwo → przechył → timers → Start) fits on one screen without scrolling on a P20-class device. Extend the H1 sizing seam. Unit-test any pure sizing math; one-screen fit is `🔬`. Refines H1. | 🔬 |

| K4 | **Share a ride as a graphic from Route detail.** In Route detail, add a **Share** action that produces a shareable summary card — the route's mini-map/track thumbnail + key stats (name, date, distance, duration, max speed, bike) rendered to an image and shared via the Android share sheet (`FileProvider`, alongside the existing GPX export). Unit-test the card-content/model + share-intent building; rendered image + share sheet are `🔬`. | 🔬 |

| K5 | **Fetch weather once at ride start (online only).** On starting a recording, make a **single** weather API call for the start location and store it in the route's `wxJson` (existing shape `{"temp":…,"hum":…,"rain":…}` that `RouteWeather.parse` / Route detail already render). **Gate on connectivity:** only fetch when online AND the app is **not** in offline / `offlineOnly` mode (per `NetworkMonitor` + settings) — otherwise skip and leave `wxJson` null (today's behaviour). No continuous polling — one call at start (optionally one at end). New `WeatherClient` interface behind a fake (unit-testable), like the other network clients (A7/A10); pick a simple current-conditions API. Unit-test: online+not-offline → client called + wxJson populated/parsed; offline/offlineOnly → no call, wxJson null; parse into the existing weather shape. Depends on A6/A7 infra + `NetworkMonitor`. | ✅ |

| K6 | **Compass: fixed dial, only the red needle rotates.** Currently the whole compass dial (E6) rotates — the N/E/S/W bezel spins so the letters appear at angles. Change to the conventional layout: **the dial/bezel stays fixed** (N at top always, cardinal labels upright and static) and **only the red heading indicator (needle/line) rotates** to point at the current heading (degrees). Update `CompassDial` so the rose is static and just the needle's rotation is driven by the heading. Unit-test any needle-angle math; visual is `🔬`. Refines E6. | 🔬 |

| K7 | **GPS satellite indicator: live in Idle, colour-coded, centered, sole chip in the top row.** (a) **Live before recording:** today location/GNSS only starts on Start/Resume (`startLocationUpdates()`), so the sat count is static "0 sat" in Idle — start a GNSS status listener in **Idle** too so the rider can wait for a fix before starting (needs location permission; degrade gracefully if denied). (b) **Colour:** **red when 0 satellites**, amber while acquiring (1–3), and the **speed/accent colour once there's a usable fix (≥4)**. (c) **Layout:** **center the satellite indicator** in the top row and **remove the other chips** from that row (weather / driving range) — that info is available elsewhere/below. Refines G1/H5/F2. Unit-test the count→colour thresholds + the Idle-GNSS gating; on-device sat behaviour is `🔬`. | 🔬 |

## L. Recording correctness bugs (on-device feedback 2026-07-15, round 7)

| #  | Item | Status |
|----|------|--------|
| L1 | **Route-list map thumbnail missing for a freshly recorded route (BUG).** Root cause confirmed: `RouteCard` was drawing a hardcoded empty placeholder and never rendering `thumbnailPathD`. **Fixed:** added `RouteThumbnail.parsePathD` (pure parser, unit-tested), replaced the placeholder `Box` with `RouteThumbnailImage` composable that draws a Canvas polyline from the path data, letterboxed into 100×64 dp. Regression screenshot test `routes_cockpit_thumbnail_rendered` captures the rendered thumbnail and guards against re-regression headlessly. Pixel-perfect look on real device 🔬. | ✅ |
| L2 | **GPS satellite count shows 0 during recording although GPS works (BUG).** The "GPS · N sat" indicator reads 0 even while recording successfully captures a track — i.e. the count is **never sourced from real GNSS status**. **Fix:** register a `LocationManager.registerGnssStatusCallback` (GnssStatus) and feed the used-in-fix / total satellite count into the recording UI state, active during recording (and Idle per K7). Unit-test the GnssStatus→count mapping; on-device count is `🔬`. Root cause behind K7's colour/idle work. | 🔬 |
| L3 | **Verify + ensure recording continues with the screen OFF (CRITICAL).** User impression: with the screen off the ride may stop recording. A ride tracker MUST keep logging with the screen off / app backgrounded / phone in pocket. **Fix/verify:** location collection must be owned by the **foreground service** (started, `FOREGROUND_SERVICE_LOCATION`, ongoing notification), independent of the Activity/Compose lifecycle and `viewModelScope`; it must survive screen-off and Doze (partial wake lock / foreground-service exemption). Today `startLocationUpdates()` collects in the VM scope — confirm whether the flow is actually driven by the service and doesn't pause when the UI stops. Unit-test the service-owned collection seam; real screen-off logging is `🔬` (on-device). | 🔬 |

## M. Location regression from L3 + live idle speed/altitude (on-device feedback 2026-07-16, round 8)

| #  | Item | Status |
|----|------|--------|
| M1 | **CRITICAL REGRESSION from L3 — NO GPS is recorded at all (zero track, speed, distance, elevation).** Confirmed on-device + in the DB: every route recorded on the K+L build has **0 trace chunks, km=0, max=0, elev=0, empty speedJson/elevProfileJson**, while lean/compass/duration ARE recorded and the **satellite count works**. Older (pre-L3) routes have full tracks. **Root cause (verified in code):** `FusedLocationClientImpl.locationUpdates()` (`data/location/LocationClient.kt`) registers the `LocationListener` via the 4-arg `LocationManager.requestLocationUpdates(GPS_PROVIDER, intervalMs, 0f, listener)` overload, which binds the callback to the **calling thread's Looper**. Before L3 the flow was collected on `viewModelScope` (main thread → has a Looper → worked). L3 moved collection into `RideLocationCollector` running on `CoroutineScope(SupervisorJob() + Dispatchers.Default)` (`di/RecordingModule.kt`), a **pool thread with no Looper**, so `requestLocationUpdates` throws/never delivers and the collector job dies silently under the SupervisorJob. GNSS status still works because `AndroidGnssStatusClient` registers with an explicit main Looper/Executor. **Fix:** make GPS callback delivery independent of the collecting thread — use the Looper/Executor-taking overload, e.g. `requestLocationUpdates(GPS_PROVIDER, intervalMs, 0f, listener, Looper.getMainLooper())` (or the `Executor` overload with a main/HandlerThread executor). Keep the collector on `Dispatchers.Default` so the L3 screen-off/Doze design is preserved. Also consider requesting from `FUSED_PROVIDER`/`NETWORK_PROVIDER` as a fallback when GPS is cold, but GPS_PROVIDER with a proper Looper is the fix. **Add a regression unit test** proving `locationUpdates()` collected from a non-main / no-Looper dispatcher still delivers samples (fake `LocationManager` seam, or assert the overload with an explicit Looper/Executor is used). Real fix confirmation is on-device `🔬` (record a short ride → route has non-zero km + trace chunks). Fixes the L3 regression. | ✅ |
| M2 | **Live speed + altitude in Idle (before recording), like the compass/lean/satellites already are.** Today speed and altitude read 0 on the Idle screen because location samples are only consumed while Recording (`startLocationUpdates()` runs on Start/Resume) and the engine only aggregates during a ride. **Fix:** add an always-on collector of `RideLocationCollector.samples` in the VM `init` (sibling to the existing always-on lean/heading/GNSS collectors) that updates **live** speed and altitude fields in `RecordingUiState` regardless of phase, so the Idle cockpit shows real-time speed + altitude once there's a fix — WITHOUT feeding the `RecordingEngine` (no distance/track accumulation until the rider taps Start). Ensure GNSS/location is actually streaming in Idle (`startGnss()` already is; GPS samples require the collector's GPS `start()` — decide whether to start GPS in Idle too, or drive the live fields from a lightweight location subscription). Depends on M1 (samples must actually flow). Unit-test: samples in Idle → uiState live speed/altitude update, engine NOT advanced (km stays 0 until Start). On-device live readout `🔬`. | ✅ |

*(M depends on M1 landing first — M2's live readout is meaningless until GPS samples flow again.)*

## N. GPS track-quality improvements (best-practice, feedback 2026-07-17, round 9)

| #  | Item | Status |
|----|------|--------|
| N1 | **Store per-point timestamp + elevation in the recorded track (GPX-grade points).** Today each track point persists only `lat,lng` (see `RecordingEngine.buildPathJson` / `TraceChunkCodec`), which loses per-point time and altitude — preventing accurate playback, per-location speed, pause reconstruction, and standards-compliant GPX/TCX export. **Fix:** extend the persisted track point to `{lat, lng, ele, t}` where `ele` = altitude (m) and `t` = epoch-ms of the fix (thread `timeMs` from `LocationSample`, already captured). Update `RecordingEngine.pathPoints` to hold the richer point, `TraceChunkCodec` serialization, the D11 chunk storage, the resume/append seeding (J5), and the GPX exporter to emit `<time>`/`<ele>`. **Backward-compat: version the chunk format** — old `lat,lng`-only chunks (and routes) must still parse (default `ele=0`, `t=null`), so existing 30 routes keep rendering; only new rides get full points. Map rendering (`OsmTrackMap`, thumbnails) reads lat/lng as before. Unit-test: round-trip of enriched points through codec + chunk store; legacy lat/lng-only chunk still decodes; GPX contains time+ele for new rides. On-device replay/export `🔬`. | ✅ |
| N2 | **Reject low-quality GPS fixes (accuracy + outlier filtering), not just the speed gate.** The current `MOVING_THRESHOLD_KMH = 2.0` gate stops parked jitter but does NOT reject a poor-accuracy fix while moving (e.g. a 100 m error spike that teleports the track). **Fix:** thread horizontal accuracy (`Location.getAccuracy()`) into `LocationSample` and, in `RecordingEngine.onLocation`, drop fixes whose accuracy is worse than a threshold (start ~35 m, tunable const) and reject implausible jumps (implied speed between consecutive kept fixes above a sane cap, e.g. > 300 km/h → discard as outlier). Dropped fixes must not advance distance/track/elevation but may still update the live speed readout cautiously. Keep it pure + unit-testable in the engine (no Android types leaking): add `accuracyM` to `LocationSample`, add the filter with named constants. Unit-test: bad-accuracy fix dropped (no distance added); teleport outlier dropped; good fixes accumulate normally; threshold boundaries. On-device smoothness `🔬`. Refines E5. | ✅ |

*(N1 changes the on-disk track-point format — MUST stay backward-compatible with the existing lat/lng-only chunks; version the codec, do not migrate/rewrite old routes.)*

## O. Recording reliability (feedback 2026-07-17, round 10)

| #  | Item | Status |
|----|------|--------|
| O1 | **Exempt the app from battery optimization / background-kill so screen-off recording actually survives (esp. Huawei/EMUI).** L3 made location collection service-owned, but on aggressive OEMs (the target device is a Huawei P20 / EMUI, Android 9) the OS still kills the foreground service unless the app is whitelisted from Doze/battery optimization and OEM "protected apps / auto-launch". **Fix:** (a) add the `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` permission; (b) provide a checker seam `isIgnoringBatteryOptimizations()` wrapping `PowerManager.isIgnoringBatteryOptimizations(packageName)`; (c) when a recording is about to start (or via a Settings entry) and the app is NOT exempt AND the user hasn't dismissed the prompt, show a dialog explaining why (uninterrupted screen-off logging) with an action that fires `Intent(ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS)`; (d) include a secondary "open app auto-launch / protected-apps settings" hint for EMUI/MIUI (best-effort OEM intent with graceful fallback to the generic battery-optimization settings screen — never crash if the OEM activity is absent); (e) persist a `batteryPromptDismissed` flag in DataStore so it isn't nagged every ride. All user-facing copy as string resources in **all 6 languages** (pl/en/de/fr/cs/ru). Unit-test the pure gate logic (exempt → no prompt; not exempt + not dismissed → prompt; dismissed → no prompt) behind an injectable checker; the actual system dialog + OEM intents are `🔬`. Complements L3. | 🔬 |
| O2 | **Handle GPS signal loss gracefully (no straight-line teleport across a dropout).** When GPS drops for a stretch (tunnel, urban canyon, phone in a bag) and then reacquires, the next kept fix can be far from the previous one; today `RecordingEngine.onLocation` would add that whole jump to the odometer as one straight segment, and the map would draw a long false straight line. **Fix:** in the engine, detect a dropout by the elapsed time since the last accepted fix (e.g. gap > `GPS_GAP_SEC` ≈ 20 s, tunable const) OR an implausible inter-fix jump; when a dropout is detected, **do NOT accumulate the bridging distance/elevation across the gap** and start a **new track segment** at the reacquired fix so rendering does not connect across the gap. Represent segment breaks without hard-depending on N1 (e.g. an engine-level list of break indices, or reuse N1's per-point time if that has landed) and make `OsmTrackMap`/thumbnail rendering draw each segment as a separate polyline. Keep detection pure + unit-testable (no Android types): drive it off `LocationSample.timeMs` (already present) and inter-fix distance. Unit-test: normal fixes → single segment, distance accumulates; a > `GPS_GAP_SEC` gap → new segment, bridging distance NOT added; multiple gaps → multiple segments. On-device tunnel behaviour `🔬`. Complements N2 (per-fix outliers) — O2 is the multi-second-dropout case. | 🔬 |

*(O1 is the highest-impact reliability item on the actual test device; O2 keeps the odometer/track honest when GPS drops. O2 should compose with N1/N2 but must not hard-block on them.)*

## P. Coordinate readout + configurable coordinate format (feedback 2026-07-18, round 11)

| #  | Item | Status |
|----|------|--------|
| P1 | **Show live latitude/longitude in the top GPS row (Idle + Recording), fitting on-screen with nothing truncated.** The top status row currently shows only the satellite count (`GpsChip(satCount, onRoad)` in `RecordingScreen.kt`, fed by `state.gpsSatCount`). **Add the current coordinates** next to / under the sat count so the rider can read their position. **Wire the live location:** add `liveLat: Double?` + `liveLng: Double?` to `RecordingUiState` and feed them from the same always-on `RideLocationCollector.samples` collector that M2 uses for `liveSpeedKmh`/`liveAltitudeM` (populated regardless of phase; null until the first fix → show a placeholder like "—" or "brak fiksa"). **Layout is critical:** the coordinate string (esp. UTM, which is long — see P2) MUST fit the P20-class width with **no clipping/ellipsis** — use a responsive layout (smaller mono font, wrap sat-count + coords onto their own lines if needed, `maxLines`+`autosize`/`FittedText` rather than truncation). Format the value via the P2 formatter (default decimal degrees until P2 lands). Unit-test the VM live-lat/lng wiring (sample → uiState) and any fit/measure helper; on-screen no-clip is `🔬`. Depends on M2 (always-on collector) + P2 (formatter). Refines K7/L2. | 🔬 |
| P2 | **Settings option: coordinate display format (Decimal Degrees / DMS / UTM …).** Add a Settings picker (like the existing theme/units pickers) letting the user choose how coordinates are rendered wherever the app shows a lat/lng (the P1 top-row readout first; also any route-detail coordinate text). **Formats:** at minimum **Decimal Degrees** (e.g. `51.26059, 15.56916`), **Degrees-Minutes-Seconds** (e.g. `51°15'38"N 15°34'09"E`), and **UTM** (e.g. `33U 508900 5680230`); optionally Decimal Minutes / MGRS. Persist a `coordFormat` enum in DataStore (`SettingsDataStore` key + `AppSettings` field + `SettingsStore` setter, defaulting to Decimal Degrees) and thread it into `RecordingUiState`/the formatter. **Provide a pure, unit-testable `CoordinateFormatter`** (lat, lng, format → String) with a **correct WGS-84 → UTM** implementation (zone number + latitude band + easting/northing; handle both hemispheres, the standard false-easting 500 000 / false-northing 10 000 000 for southern band, and near-zone-boundary values). All picker labels/format names as string resources in **all 6 languages** (pl/en/de/fr/cs/ru). Unit-test each format against known reference coordinates (incl. a UTM value cross-checked to a known conversion, southern hemisphere, and a zone boundary); the on-screen rendering is `🔬`. | ✅ |

*(P1's layout must never clip — UTM strings are the widest; size/wrap the row to fit the narrowest supported screen. P2's UTM math must be verified against known reference conversions, not just round-tripped.)*

## Q. Stats gaps + TCX/FIT export (feedback 2026-07-18, round 12)

| #  | Item | Status |
|----|------|--------|
| Q1 | **Fill the two real gaps in the existing Statistics screen: total fuel + cost, and a lean-angle histogram.** The `StatsScreen` (`ui/screens/stats`, fed by `StatsViewModel`/`StatsUiState`, using `BikeStatsCalculator` + `PersonalRecordsCalculator`) ALREADY shows total distance, time-in-saddle, ride count, top-speed record, monthly bars, riding-style (avg lean, avg speed, total climb), records + badges — do NOT rebuild those. **Add (a) a `total fuel + total cost` aggregate** across all rides (sum `route.fuel`; cost = Σ `route.fuel × route.fuelPricePerL`, falling back to the bike's configured price when a route has none), as a new stat tile with the active currency. **Add (b) a lean-angle histogram** — the distribution of riding time (or distance) across lean-angle buckets (e.g. 0–10°, 10–20°, 20–30°, 30–40°, 40°+, split L/R or absolute), rendered as a small bar chart on the Stats screen. **The histogram needs new capture:** today the engine only keeps `maxLeanLeftDeg/RightDeg`, not a distribution — add a per-ride lean-bucket accumulator in `RecordingEngine` (increment the bucket for the current lean on each `onLean`/tick while Recording) and persist it per route (new column or small JSON, backward-compatible: old routes have no histogram → the Stats screen aggregates only rides that have one, and shows an "insufficient data" state until then). Provide pure, unit-testable calculators (routes → total fuel/cost; lean samples → bucket counts; per-route buckets → aggregated histogram). All new labels in **all 6 languages**. Unit-test the fuel/cost aggregation (incl. price fallback + missing data) and the bucket math; on-screen chart is `🔬`. Extends E2/K1 stats; the histogram capture is new per-route data (not retro-applied). | 🔬 |
| Q2 | **Export a ride as TCX (and, if feasible, FIT) alongside the existing GPX.** Route detail already exports GPX (`core/format/GpxExporter`, shared via `FileProvider`) — now enriched with `<time>`/`<ele>` per N1. **Add TCX export** (`TcxExporter`): a Garmin Training Center XML `<Activity sport="Other">` with a `<Lap>` + `<Track>` of `<Trackpoint>`s carrying `<Time>`, `<Position>` (lat/lon), `<AltitudeMeters>`, and `<DistanceMeters>` (cumulative), plus total time/distance/max+avg speed — reuse the N1 per-point `t`/`ele` and the route's `speedJson`; points lacking `t` (legacy routes) fall back to interpolated/synthetic timestamps or are exported without `<Time>` (document which). Wire a format choice into the existing export/share action (GPX | TCX) with the same `FileProvider` share sheet. **FIT is optional/stretch** — it is a binary format needing a proper encoder (record messages, CRC); implement a minimal FIT encoder only if clean, otherwise ship GPX+TCX and leave FIT as a follow-up (say so in a `log`/comment, don't half-ship it). Provide a pure, unit-testable `TcxExporter` (route + points → XML string) tested against the schema shape + a known fixture; the share sheet is `🔬`. Depends on N1 (per-point time/ele). | ✅ |

*(Q1: reuse the existing Stats screen/calculators — only ADD the fuel/cost tile + lean histogram; the histogram requires new per-ride capture and won't populate for pre-Q1 routes. Q2: GPX is done; TCX is the concrete deliverable, FIT only if the encoder is clean.)*

## R. Manual fuel-level correction (feedback 2026-07-18, round 13)

| #  | Item | Status |
|----|------|--------|
| R1 | **Let the rider manually correct the current tank level (fuel that left the tank without riding — siphoned to a friend, lawnmower, spillage, evaporation).** Today remaining fuel is DERIVED, not stored: `remaining = tankCapacity − distanceSinceLastFull × consumption/100` (`RecordingEngine`), re-anchored only by `fillToFull()`. So off-ride fuel loss is invisible and the estimate stays too high until the next full tank. **Add a fuel-level correction** that re-anchors the remaining-fuel baseline. **UI (decided): one dialog with a toggle** — (a) **"Ustaw na"** = set the current tank level to an absolute X litres; (b) **"Zmień o"** = apply a signed ±X litre delta to the current estimate (e.g. −3 L for the lawnmower). Reachable **both** from the recording controls (next to the refuel action) **and** from the bike detail screen (corrections happen off-ride too, so it must NOT require an active route/session). **Model:** persist a new `fuel_adjustment_event` (Room table + migration v7→v8, backward-compatible; per-bike, nullable routeId, epochMs, mode = SET_ABSOLUTE|DELTA, litres value) and generalise the engine anchor: `remaining = anchorLitres − distanceSinceAnchor × consumption/100`, where a full fill anchors to `cap`, "Ustaw na" anchors to X, and "Zmień o" anchors to `currentRemaining ± delta`. **Critical:** the correction MUST be **excluded from the H4 consumption (L/100km) ledger** — it is not distance-driven consumption and must not corrupt the computed consumption; only full-to-full refuels feed H4. Remaining-fuel, range, low-fuel warning, and the H3 icon colour all read the new anchor. All dialog copy in **all 6 languages** (pl/en/de/fr/cs/ru). Unit-test: SET_ABSOLUTE anchors remaining to X; DELTA shifts it by ±L (and clamps at 0 / cap); the correction is ignored by the consumption calculator; range/colour reflect the corrected level; migration preserves data. On-screen dialog is `🔬`. Extends E3/E4/G5/H3/H4. | 🔬 |

*(R1 adds a second kind of fuel anchor alongside fill-to-full — it changes the remaining-fuel baseline but must stay OUT of the consumption ledger, and must work off-ride from the bike screen, not only during a recording.)*

## S. Track-quality fixes + network location (on-device feedback 2026-07-18, round 14)

| #  | Item | Status |
|----|------|--------|
| S1 | **Put the coordinates on the SAME row as the satellite count (sat left, coords right) — don't add a second row that shifts the whole screen down.** P1 rendered the live lat/lon as a **new line under** the GPS sat count, pushing the rest of the Recording layout down. **Fix:** make the top GPS row a single row — **satellite count left-aligned, coordinates right-aligned** on the same line (`Row` with `Spacer(Modifier.weight(1f))` between, or a `SpaceBetween` arrangement). The coordinate text stays no-clip (smaller mono font / responsive sizing per P1) but must NOT occupy its own extra row. The row height must equal the previous single-chip height so the Idle/Recording layout no longer shifts. Unit-test any layout-arrangement helper; visual is `🔬`. Refines P1. | ✅ |
| S2 | **Stop O2 from breaking the track into pieces on a normal stop (only split on a REAL GPS dropout).** On-device evidence (a 0.73 km / 487 s walk, 569 points): median inter-point gap 1.0 s, but **12 gaps > 20 s (max 158 s) → O2 produced ~13 segments**, so the map drew a broken, non-continuous line. Root cause: the >20 s gaps are NOT GPS dropouts — they come from stationary stretches where the moving-gate (`MOVING_THRESHOLD_KMH = 2.0`) and/or the N2 accuracy filter drop runs of fixes; `TrackSegmenter`/`RecordingEngine` then treat the time gap alone as a dropout and start a new segment even though the rider barely moved. **Fix:** a segment break must require **both** a time gap > `GPS_GAP_SEC` **and** a significant spatial jump between the two kept points (reacquisition distance, e.g. > `REACQUIRE_DIST_M` ≈ 60 m) — a pause where recording resumes near the same spot stays ONE continuous segment. Keep the odometer honest (still don't bridge distance across a genuine far-jump dropout). Update `TrackSegmenter` (+ the engine's gap logic) and its unit tests: stationary pause (big time gap, tiny distance) → single segment; true dropout (big time gap AND big distance) → split; verify the walk-like fixture yields ONE segment. Fixes the O2 over-segmentation. Refines O2. | ✅ |
| S3 | **Reduce low-speed track jaggedness (light smoothing).** Same walk: steps median 1.2 m, **0 steps > 15 km/h** (so this is NOT leftover N2 outlier jitter — it's inherent GPS wander at walking/idle speed that we never smooth). **Fix:** apply a light, pure, unit-testable smoothing to the track used for rendering/thumbnail (and optionally the stored path) — e.g. a small moving-average / Douglas–Peucker simplification, or a minimum-move gate (don't append a new point unless it is > `MIN_POINT_MOVE_M` ≈ 3 m from the last kept point) — tuned so genuine turns are preserved but stationary/low-speed zigzag is removed. Must not over-smooth corners or shorten distance materially. Keep legacy routes rendering unchanged if smoothing only applies going forward, or apply at render time (decide + document). Unit-test the smoother (straight run preserved, jitter cluster collapsed, corner retained); on-screen smoothness `🔬`. Complements N2/O2. | ✅ |
| S4 | **Add network (cell/Wi-Fi) location as a supplementary position source.** Today `FusedLocationClientImpl` requests only `LocationManager.GPS_PROVIDER`, so before a GPS fix (cold start, indoors, urban canyon) there is no position at all. **Fix:** also register `NETWORK_PROVIDER` (when present/enabled) and merge it with the GPS stream in `LocationClient`/`RideLocationCollector`, so the app can show/seed a coarse position from cell+Wi-Fi until a real GPS fix arrives, and fall back to it when GPS is lost. **Prefer the more accurate source** (network fixes carry large `accuracyM`, so during a ride N2's accuracy filter will still reject them from the recorded track — that's correct; the network fix is mainly for the Idle "waiting for fix" readout and as a fallback, not for the precise recorded trace). Thread `accuracyM`/provider through so consumers can prefer GPS when both are fresh. Handle `NETWORK_PROVIDER` absent/disabled gracefully (no crash). Unit-test the merge/prefer-more-accurate logic behind the injectable seam; on-device network-fix behaviour `🔬`. Depends on N2 (accuracyM already on `LocationSample`). | 🔬 |

*(S2 is the concrete "broken line" fix — split only on time-gap AND distance-jump. S3 addresses the residual jaggedness. S4 adds a coarse fallback position; it must not pollute the precise recorded track — N2's accuracy gate keeps network fixes out of the odometer.)*

## T. Splash screen + fuel-in-Idle bug (on-device feedback 2026-07-18, round 15)

| #  | Item | Status |
|----|------|--------|
| T1 | **Add a branded startup/splash screen while the app initializes (instead of the blank white screen).** On cold start (and especially right after an install when Room runs migrations) there is a noticeable blank/white window before the UI appears. **Add a splash screen** shown during initialization that (a) is visually distinctive — a **motorcycle vector/SVG animation** (e.g. the existing `icon.svg` moto mark animated: spinning wheels / riding forward / throttle pulse — a lightweight Compose `AnimatedVectorDrawable`/vector animation, no heavy raster assets), and (b) **shows a short status line of what's happening** ("Ładowanie…", "Migracja bazy…", "Wczytywanie tras…") reflecting the actual init step where feasible. Target Android 9+ (no reliance on the API-31 SplashScreen API — use a first-composable/Activity splash that dismisses when init completes or after a min/max duration). Status copy in **all 6 languages** (pl/en/de/fr/cs/ru). Unit-test any init-state → status-label mapping / min-duration gate; the animation + render are `🔬`. | 🔬 |
| T2 | **Fix phantom "fuel = 0" + the low-fuel warning ROW showing in Idle, and stop the warning from shifting the layout.** Two coupled problems, confirmed in code: (1) **Bug** — in Idle (before Start), `RecordingEngine.reset()` hasn't run, so `anchorLitres` is still its field default `0.0` while `updateFuelConfig` has set a non-null `tankCapacityL`; the snapshot then computes `remaining = (0.0 − 0).coerceAtLeast(0) = 0` → the tank reads **0** and `lowFuel = 0 ≤ 0.15·cap = true`, so the low-fuel warning fires with a full tank sitting in the garage. **Fix:** when a tank capacity is set and no fuel anchor/session has been established yet, initialize the Idle fuel baseline to full (`anchorLitres = cap`) — e.g. have `updateFuelConfig` seed `anchorLitres` to `cap` when the engine is in the pristine/Idle state — so Idle shows the assumed-full level (correctable via R1) and does NOT false-alarm. (2) **UI** — the user does not want the low-fuel **warning as its own row** (`RecordingScreen.kt` ~L601, `if (metrics.lowFuel) { … warn_low_fuel … }`): it adds a line that pushes the other elements and overflows the screen. **Fold the low-fuel signal into the existing fuel icon** (H3 already tints it red at low range) — e.g. red icon + optional small badge/pulse — and **remove the separate full-width warning row** so the Recording layout no longer shifts/overflows. Unit-test the Idle-baseline fix (cap set, Idle → remaining = cap, lowFuel = false) and the fuel snapshot; the on-screen row removal is `🔬`. Fixes an E3/H3/R1 interaction. | ✅ |

*(T2 (1) is a real bug — a configured bike shows an empty tank + low-fuel alarm before you even start; the fix seeds the Idle baseline to full. T2 (2) removes the layout-shifting warning row and conveys low fuel via the icon instead.)*

*(section format: the architect ticks `✅` once fixed + unit-tested + clean `assembleDebug`/`lintDebug`; on-device confirmation is `🔬`.)*

*(section format: the architect ticks `✅` once fixed + unit-tested + clean `assembleDebug`/`lintDebug`; on-device confirmation of the permission dialog is `🔬`.)*

## U. Consolidate the offline / network settings (feedback 2026-07-18, round 16)

| #  | Item | Status |
|----|------|--------|
| U1 | **Replace the three overlapping network flags with one clear model in a single place.** Today there are three confusing, overlapping settings spread across two tabs: `offline` ("Tryb offline" under Serwer — gates sync upload + weather), `offlineOnly` ("Praca bez internetu" under System i prywatność — hard-blocks sync + weather + OSRM correction + online group), and `auto_sync` (also gates sync). Users cannot tell them apart (confirmed on-device). **Consolidate into one "Sieć/Network" section with two clear controls:** (1) a master **"Tryb bez internetu"** switch = today's `offlineOnly` semantics (blocks ALL network: sync, weather, OSRM correction, online group); and (2) a **"Synchronizuj z serwerem"** switch that replaces BOTH `offline` (inverted) and `auto_sync` for the upload path — greyed-out/forced-off while the master "no internet" is on. Weather stays one-shot online-only (K5) and must be gated only by the master switch + real connectivity (drop the separate `offline` term from its gate). **Update every gate site consistently** (`SyncRepositoryImpl`, `GpsCorrectionRepositoryImpl` drain, weather fetch in `RecordingViewModel`, route-detail "offline" weather label, the "saved offline" toast) to read the new model. **DataStore migration (critical):** map the existing keys to the new ones so current values are preserved — e.g. new `noInternet = old offlineOnly`; new `syncEnabled = !old.offline && old.auto_sync` (verify the intended truth table so a user who was fully offline STAYS fully offline after upgrade). Keep the old keys readable for one version or migrate-and-drop; either way no setting silently flips. Remove the now-dead second offline toggle from its old tab. All new/changed labels + descriptions in **all 6 languages** (pl/en/de/fr/cs/ru). Unit-test: the migration/truth-table mapping (each combination of old offline/offlineOnly/auto_sync → correct new noInternet/syncEnabled), and that every gate (sync, weather, correction, group) reads the new model; the on-screen single-section layout is `🔬`. Supersedes the split offline flags. | ✅ |

*(U1 is a settings refactor touching many gate sites — the DataStore migration MUST preserve each user's current effective behaviour, especially "fully offline". Verify the old→new truth table with unit tests before shipping.)*

## V. Tap coordinates to copy (feedback 2026-07-18, round 17)

| #  | Item | Status |
|----|------|--------|
| V1 | **Tap the coordinate readout to copy it to the clipboard; double-tap to copy a Google Maps link.** On the GPS status row (the coordinate text added in P1/S1, rendered via the P2 `CoordinateFormatter`): **single tap** copies the current position to the clipboard **in all three supported formats, each labelled**, as multi-line text, e.g.:\ `DD 51.26059, 15.56916` / `DMS 51°15'38"N 15°34'09"E` / `UTM 33U 508900 5680230` — then shows a toast "Współrzędne skopiowane do schowka". **Double tap** copies a **Google Maps link** to the current position (e.g. `https://www.google.com/maps/search/?api=1&query=<lat>,<lng>`) and shows a toast "Link Google Maps skopiowany". Use `detectTapGestures(onTap = …, onDoubleTap = …)` on the coordinate composable, the existing `CoordinateFormatter` for the three formats, and a `ClipboardManager`. **No-op gracefully when there is no fix yet** (liveLat/liveLng null → don't copy, optional "brak pozycji" toast). Provide a pure, unit-testable builder: `(lat, lng) → labelled 3-format clipboard string` and `(lat, lng) → maps URL`; the gesture + toast + actual clipboard write are `🔬`. All toast/label strings in **all 6 languages** (pl/en/de/fr/cs/ru). Depends on P1/S1 (coordinate readout) + P2 (formatter). | ✅ |

*(V1 single-tap = all 3 labelled formats to clipboard + toast; double-tap = Google Maps URL + toast. Keep the copy-text builders pure/unit-tested; guard the no-fix case.)*

## W. Editable OSRM correction-server address in Settings (feedback 2026-07-18, round 18)

| #  | Item | Status |
|----|------|--------|
| W1 | **Expose the GPS-correction (OSRM) server address as an editable Settings field.** The data layer is already complete — `AppSettings.osrmBaseUrl` (default `http://192.168.1.142:5001`), the `osrm_base_url` DataStore key with getter, `SettingsStore.setOsrmBaseUrl(...)` (wired in `SettingsDataStore`), and it is included in backup/restore — but there is **NO UI to edit it**; only the GPStrack `serverAddress` is exposed in Settings. **Fix:** add a text field in the Settings server/network section (mirror the existing GPStrack server-address field) that shows and edits `osrmBaseUrl`, wired to `viewModel::setOsrmBaseUrl` and `state.osrmBaseUrl`. Place it alongside the GPStrack server address (and, once U1 lands, within the consolidated "Sieć" section). Validate/trim the URL (basic `http(s)://host:port` sanity; empty → restore default) and show the current value as the field/subtitle. Label + description in **all 6 languages** (pl/en/de/fr/cs/ru). Optionally a "test connection" affordance is out of scope here. Unit-test the VM set/expose seam (setOsrmBaseUrl persists; state reflects it) + any URL-normalisation helper; the on-screen field is `🔬`. Depends on the existing `osrmBaseUrl` plumbing; compose with U1's settings reorg. | 🔬 |

*(W1 is UI-only wiring — the osrmBaseUrl model/DataStore/setter already exist; just surface an editable field next to the GPStrack server address. Fits inside U1's consolidated network section once that lands.)*

## X. Waves by encounter + group roster (design 2026-07-18, round 19)

*(Brainstormed design: waves are sessionised into encounters by presence-continuity; a configured group is treated as one "rode together" entry. Purely local BLE — no internet, no accounts. SMS/web group-invite explicitly OUT of scope.)*

| #  | Item | Status |
|----|------|--------|
| X1 | **Encounter model core: `EncounterTracker` + data model + migration (replace the 5-min dedup).** Today `WaveDedupTracker` debounces the same `shortId` to one wave per 5 min, treating a whole-ride companion and a passed-3×-a-day rider identically. **Replace it with presence-continuity sessionisation.** Add a pure, unit-testable **`EncounterTracker`**: per `shortId` it holds `firstSeen`/`lastSeen`; on a sighting at T, if there is no open encounter OR `T − lastSeen > gapMs` it opens a **new encounter** (emits a "started" event) else it **extends** the current one (updates `lastSeen`). `gapMs` = `inGroup ? ∞ : encounterGapMinutes` — so a group member is ONE encounter for the whole ride, a stranger splits after the gap. **Data:** new Room table **`RiderEntity`**(`shortId` PK, `nick`, `bike`, `lastSeenMs`, `inGroup` Bool) upserted on every sighting; extend **`WaveEntity`** with `shortId`, `firstSeenMs`, `lastSeenMs` (one row per encounter; legacy rows default first=last=`time`). Room migration **v9→v10** (new table + additive columns, backward-compatible, existing data preserved). New setting `encounterGapMinutes` (default 10) in DataStore/AppSettings. Rewire `AndroidBleWaveSource` to emit **raw sightings** (light per-`shortId` throttle ~few s) instead of the 5-min pre-dedup; the service upserts `RiderEntity` and feeds `EncounterTracker`, which drives wave-row insert (on started) / update-`lastSeenMs` (on extended). Delete `WaveDedupTracker`. Unit-test: companion with sub-gap dropouts → 1 encounter; stranger with >gap absences → N encounters; group member (gap ∞) → 1 encounter/ride; migration test (Robolectric) preserves data. On-device 🔬. | ✅ |
| X2 | **Group roster: manage from the recording screen (in-range riders) and in Settings.** Group membership is the `RiderEntity.inGroup` flag (persistent, per `shortId`). **Recording screen:** add a **person icon** that opens a sheet of riders **currently in range** (sighted within the last ~N s, from the live BLE stream) each with an add/remove-from-group toggle — usable at a stop or during recording, live. **Settings (Pomachania):** a **group editor** listing all known riders (`RiderEntity`) with `inGroup` checkboxes (view/edit the persistent roster), plus a **"Traktuj grupę osobno"** master toggle that enables/disables special group handling (when off, group members are treated as ordinary encounters). All labels in **all 6 languages** (pl/en/de/fr/cs/ru). Unit-test the in-range filter (last-seen window) + the inGroup toggle seam; the sheet/checklist render is 🔬. Depends on X1. | 🔬 |
| X3 | **Wave signalling during the ride, configurable.** When an encounter STARTS (`EncounterTracker` "started" event: a new rider in range, a stranger reappearing after the gap, or a group member's first sighting of the ride), fire a single signal (haptic/short sound) — once per encounter, never repeated while the rider stays in range. **Add a Settings toggle "Sygnalizuj pomachania podczas jazdy"** (on/off); when OFF, encounters are still recorded silently (no haptic/sound). Wire the signal in `RecordingService`, gated on the toggle. Toggle label in **all 6 languages**. Unit-test the signal-decision seam (started + toggle-on → signal; extended → none; toggle-off → none); the actual haptic/sound is 🔬. Depends on X1. | 🔬 |
| X4 | **Waves list UI: GRUPA vs SPOTKANIA, with durations.** Update the waves list to render two sections: **GRUPA** — riders with `inGroup=true` seen on the route, each as one "jechaliście razem Xh Ym" entry (duration = `lastSeen − firstSeen`); **SPOTKANIA** — everyone else, one row per encounter with the time, so a rider passed 3× shows 3 rows. Derive duration/labels from the `WaveEntity` first/last timestamps; a very short encounter reads as "minięcie", a long one as "jechaliście razem". Keep legacy waves (single timestamp) rendering gracefully. Unit-test the grouping/label/duration mapping (pure); the composable render is 🔬. Depends on X1/X2. | ✅ |

*(X is one cohesive feature split for the loop: X1 core+data+migration, X2 group management, X3 configurable signal, X4 list UI. Group member = infinite-gap encounter is the unifying trick.)*

## Y. Periodic SMS location sharing (design 2026-07-18, round 19)

*(Separate safety feature — the phone sends its own SMS via SmsManager; purely local, no web app/accounts. Default OFF. Reuses V1's Google Maps link builder.)*

| #  | Item | Status |
|----|------|--------|
| Y1 | **Settings + recipients + message builder for SMS location sharing.** Add a Settings section **"Udostępnianie pozycji"**: an enable toggle (**default OFF**), a **recipient list picked from the phone contacts** (name + number; via the contacts picker, `READ_CONTACTS`; one or more), and an interval `smsIntervalMinutes` (default 15). Persist recipients + interval + enabled flag (DataStore or a small table). Provide a **pure, unit-testable builder**: `(recipients, lat, lng, template) → List<(number, text)>` where the text is a localized template + the **Google Maps link from the V1 builder** (e.g. "MotoTracker: jestem tu <maps-url>"). Handle `READ_CONTACTS` permission (graceful when denied — can still type nothing / feature stays off). All copy in **all 6 languages** (pl/en/de/fr/cs/ru). Unit-test the builder (multiple recipients, link format, template) + the settings seam; the contact picker + permission UI are 🔬. Depends on V1 (maps-link builder). | 🔬 |
| Y2 | **Periodic send during recording.** While a recording is active and SMS sharing is enabled, a timer in `RecordingService` fires every `smsIntervalMinutes` and sends the built messages via `SmsManager.sendTextMessage` to each configured recipient using the current GPS position — **skipping** a tick when there is no fix yet. Requires `SEND_SMS` (runtime; graceful when denied — feature disabled with a note; SMS may cost money, hence default OFF). Stops when recording stops. Keep the schedule/decision logic testable (interval elapsed + fix present + enabled → send list); the actual `SmsManager` send + timer + permission are 🔬 (on-device). Depends on Y1 + the foreground service. | 🔬 |

*(Y is local-only SMS from the device — no web/accounts. Y1 = settings/recipients/builder/permission, Y2 = the periodic send wired into the recording service. Default OFF; costs money so never silent-on.)*

## Z. Dedicated "Pomachania" settings tab + known-riders empty state (feedback 2026-07-19, round 20)

| #  | Item | Status |
|----|------|--------|
| Z1 | **Move the waves/broadcast/group settings into a dedicated "Pomachania" Settings tab, and show a known-riders empty state.** Today the whole "Rozgłaszanie (Bluetooth)" section (waves enable, "Traktuj grupę osobno", "Sygnalizuj pomachania podczas jazdy", the broadcast profile, the `encounterGapMinutes` field, and the "Znani motocykliści" group editor) lives inside the **Appearance** tab (`SettingsTab.APPEARANCE` in `SettingsScreen.kt`), which makes it hard to find. **Fix:** add a new **`SettingsTab.WAVES`** ("Pomachania") to the tab enum + tab bar and MOVE the entire broadcast/waves/group/signal block there (out of Appearance). Keep all existing wiring (`onWavesEnabled`, `onGroupTreatedSeparately`, `onSignalWavesEnabled`, `onSetRiderInGroup`, broadcast profile, gap setting) — only the tab placement changes. **Also add a known-riders empty state:** the group editor list currently only renders `if (state.knownRiders.isNotEmpty())`, so with no detected riders it looks like the feature is missing. Instead ALWAYS show the "Znani motocykliści" header, and when the list is empty show a hint row ("Brak znanych motocyklistów — pojawią się po wykryciu przez Bluetooth") so the editor is discoverable. (Confirmed: `knownRiders` = `riderRepository.observeAll()`, i.e. ALL persisted riders regardless of range — so once detected, riders stay listed here even when out of range; only the recording-screen sheet is range-filtered via `InRangeFilter`.) New tab label + empty-state string in **all 6 languages** (pl/en/de/fr/cs/ru). Unit-test any tab-content/empty-state mapping; the on-screen tab + list render are 🔬. Refines X2/X3 + the J3/B21 broadcast settings. | ✅ |
| Z2 | **Fix the splash (T1): eliminate the white cold-start flash AND redesign the visual.** Confirmed on-device: at launch a full **white screen** shows first (captured across several frames), then the Compose splash ("two circles + Uruchamianie") appears — the exact white flash T1 was meant to remove. **Root cause:** `Theme.MotoTracker` (`res/values/themes.xml`, parent `Theme.AppCompat.DayNight.NoActionBar`) sets **no `android:windowBackground`**, so the system shows a white window during process start, and the Compose `SplashScreen` only renders after Compose inflates (too late). A Compose splash CANNOT remove the pre-draw flash. **Fix (white flash):** add a **launch theme** with `android:windowBackground` = a `layer-list` drawable (app dark background `MotoTracker.colors.bg` + centered logo from `icon.svg`), apply it to `MainActivity` in the manifest, and swap to `Theme.MotoTracker` via `setTheme(...)` in `onCreate` BEFORE `setContent` — so the very first frame is the branded dark splash, never white. Make the themed windowBackground visually match the Compose `SplashScreen` background so the hand-off is seamless (no flash between them). **Fix (visual):** redesign `SplashScreen.kt` to look good in the cockpit theme — a proper **motorcycle animation** (tasteful animated vector of the moto mark: e.g. wheels rolling / headlight-to-throttle pulse, accent-coloured on the dark bg), replacing the weak "two circles"; keep the init status line but style it smaller/subtler. Verify on Android 9 (P20): cold start shows dark branded splash immediately, no white, then a nice moto animation, then the app. Unit-test any status-label mapping; the drawable/theme/animation + no-white-flash are 🔬 (on-device). Fixes T1. | 🔬 |

## AA. Branded animated splash from the new hero artwork (design 2026-07-19, round 21)

*(User-approved final splash artwork prepared as SVG. Assets in the project root: `splash_hero.svg` (static), `splash_hero_layered.svg` (animation-ready, named groups + wheel pivots), `splash_hero_hd.png` (2048px raster). Chosen approach: AVD — lightweight vector animation; the soft glow lives on the static raster background, the animation is flat-neon.)*

| #  | Item | Status |
|----|------|--------|
| AA1 | **Replace the splash windowBackground with the new hero raster (exact glow, no white flash).** Supersedes Z2's placeholder drawable. Add `splash_hero_hd.png` (the 2048px hero render) to `res/drawable*` and build a `windowBackground` layer-list: solid `#101114` fill + the hero centred (`gravity=center`, `fitCenter`/no stretch) so on a portrait phone it letterboxes on the dark background. Apply via the `Theme.MotoTracker.Launch` launch theme already wired to `MainActivity` (Z2), swapped to the app theme in `onCreate` before `setContent`. Result: cold start shows the full glowing hero instantly, zero white flash. Verify on P20 (Android 9). On-device 🔬. Fixes/replaces the Z2 splash background. | 🔬 |
| AA2 | **Convert `splash_hero_layered.svg` → Android VectorDrawable, preserving the animatable groups.** Convert the layered SVG to a `VectorDrawable` XML (`res/drawable/splash_hero_vd.xml`) — use `svg2vectordrawable` (`npx svg2vectordrawable`) or Android Studio's vector import. **Expected + acceptable:** the SVG blur filters (glow) are dropped — VectorDrawable has no blur; fills become solid `#101114/#2DD4FF/#FFFFFF/#00E676/url→solid`. **Critical — keep the named groups** as `<group android:name="...">` so AA3 can animate them: `bike`, `wheelRear`, `wheelFront`, `trail`, `pin`, `wordmark`, `mountains`. Add pivots to the wheel groups: **`wheelRear` pivotX=307 pivotY=261**, **`wheelFront` pivotX=567 pivotY=262** (viewport is 1536×440). Approximate the neon glow with a wider semi-transparent underlay stroke per coloured element if cheap, otherwise flat neon is fine. Render-check the VD matches the static look (minus glow). Unit-test any build wiring; visual 🔬. | 🔬 |
| AA3 | **Build the AnimatedVectorDrawable (6-phase-inspired, kept short ~2s) and host it in the splash.** Author `res/drawable/splash_hero_avd.xml` targeting the AA2 groups, and drive it from the existing `SplashScreen.kt` (T1) via `rememberAnimatedVectorPainter` (replace T1's placeholder vector). Phases: (1) **JAZDA** — `bike` translateX from −560→0 while `wheelRear`+`wheelFront` `rotation` 0→720° (pivots from AA2), ~0.9 s; (2) **ŚLAD/TRASA** — reveal `trail` left→right via an expanding clip (the trail is a filled shape, so use a clip-path/clip rect group animation, NOT `trimPathEnd`), ~0.7 s, overlapping; (3) **PIN** — `pin` scaleX/scaleY 0→1 + translateY −24→0 + alpha 0→1, ~0.35 s, landing at the trail end; (4) **LOGO** — `wordmark` alpha 0→1 (+ slight scale), the green "o" ring a subtle pulse, `tagline` alpha in, ~0.5 s; then a brief hold. Keep total ≈ **1.8–2.2 s** (a fast launch splash, not a 3.6 s loop) and dismiss the splash when the AVD ends OR app init completes (whichever is later, capped ~2.5 s). No blur (AVD limitation — the static `windowBackground` from AA1 carries the glow before/behind). Unit-test the phase/duration/dismiss logic (pure timing seam); the actual animation is 🔬 (on-device). Depends on AA1 (bg) + AA2 (VD groups) + T1 (SplashScreen host). | 🔬 |

*(AA1 = static glowing bg kills the white flash [primary win]; AA2 = vectorise the layered art keeping named groups + wheel pivots; AA3 = the short AVD motion [bike in, wheels spin, trail reveal, pin drop, wordmark fade]. Glow is intentionally only on the AA1 raster — AVD is flat neon by design.)*

## AB. Splash crash + no-animation fix (on-device 2026-07-20, round 22)

| #  | Item | Status |
|----|------|--------|
| AB1 | 🔬 **CRITICAL: the AA3 Compose AVD splash intermittently crashes composition and the app closes; and the animation does not play.** On-device (P20, Android 9, screen on) the app shows the static splash then **exits to the launcher**; logcat shows `E AndroidRuntime` inside Compose `setContent`/`composeInitial` followed by `ActivityManager: finishTopCrashedActivityLocked Force finishing activity com.mototracker/.MainActivity` — i.e. the splash composition throws (intermittently) and the system force-finishes the crashed activity. Root cause: the experimental Compose `rememberAnimatedVectorPainter` + `animatedVectorResource(R.drawable.splash_hero_avd)` (AA3) is fragile and also does not render group translate/rotation animations (so it only ever shows the static end state). **Fix — make the splash crash-proof and actually animate:** (1) **Remove** the `rememberAnimatedVectorPainter`/`AnimatedImageVector` usage from `SplashScreen.kt`. (2) Play the AVD via a platform `AndroidView { ImageView }` that loads `R.drawable.splash_hero_avd` and calls `(drawable as AnimatedVectorDrawable).start()` in a `DisposableEffect`/`onViewCreated` — the platform `AnimatedVectorDrawable` fully supports the group `translateX`/`rotation`/`scaleX/Y`/`alpha` animators (unlike the Compose painter), so the bike-enter + wheel-spin + trail/pin/wordmark actually animate. (3) **Wrap the drawable load + start in try/catch**; on ANY failure fall back to a plain static `Image(painterResource(R.drawable.splash_hero_hd))` (or the VectorDrawable) so a drawable problem can **never** crash composition or close the app. (4) Keep the `SplashGate` dismiss → `MotoApp` transition; verify the app **always** proceeds to the app (never `finishTopCrashedActivity`). The `windowBackground` hero (AA1) already kills the white flash and stays as the pre-draw background. Unit-test the gate/fallback decision seam; on-device (screen ON): no crash, app opens normally, and the bike/wheels/trail/pin/wordmark animate once (~2 s). Fixes AA3. | 🔬 |

## AD. Portrait animated splash — final layout (design 2026-07-20, round 24)

*(User-approved portrait layout A: scene [bike+trail+pin+mountains] upper-middle, "MotoTracker" wordmark + tagline at the bottom. Assets in project root: `splash_portrait_layered.svg` (named groups + wheel pivots), `splash_portrait_start.png` (dark #101114 + mountains only = animation start / windowBackground), `splash_portrait_hd.png` (full static hero, fallback). Portrait canvas 1080×1560. Supersedes the landscape AA/AB/AC splash assets.)*

| #  | Item | Status |
|----|------|--------|
| AD1 | **Rebuild the splash in the approved PORTRAIT layout (scene up, wordmark bottom), animated, crash-proof.** Redo the splash using the portrait assets (canvas 1080×1560): (1) **windowBackground** → `splash_portrait_start.png` (solid `#101114` + mountains only) via the `Theme.MotoTracker.Launch` theme — kills the white flash AND matches the AVD's t=0 (empty scene) so nothing is revealed before it animates. (2) **VectorDrawable** ← convert `splash_portrait_layered.svg` (svg2vectordrawable), preserving named groups `scene`, `bike`, `wheelRear`, `wheelFront`, `trail`, `pin`, `wordmark`, `mountains`; the wheel groups are nested inside `#scene` (transform `translate(2,430) scale(0.70)`) with **LOCAL pivots wheelRear=307,261 / wheelFront=567,262**. (3) **AnimatedVectorDrawable** hosted via the AB1 approach (`AndroidView`+`ImageView`+ platform `AnimatedVectorDrawable.start()`, try/catch → static `splash_portrait_hd.png` fallback, never crashes/closes the app): animate `bike` translateX −560→0 (scene-local) + `wheelRear`/`wheelFront` rotation 0→720° over ~0.9 s; `trail` reveal/fade-in ~0.7 s; `pin` scale 0→1 + drop + alpha ~0.35 s; `wordmark` alpha 0→1 (+ slight scale) at the bottom ~0.5 s; total ≈2 s, then the app opens. Verify on-device (screen ON): dark+mountains → bike rides in with spinning wheels → trail draws → pin drops → wordmark appears at the bottom → app; no white flash, no crash, no app-close. Supersedes AA1/AA2/AA3/AB1/AC1 (landscape). On-device animation is 🔬. | 🔬 |

## AE. Splash rebuilt as a layered Compose animation (on-device diagnosis 2026-07-20, round 25)

*(On-device (P20, Android 9) frame-by-frame check of AD1 found the AVD choreography does NOT play as designed: (a) the group `alpha` fade-ins for `trail`/`pin`/`wordmark` do nothing — `android:alpha` on a VectorDrawable `<group>` is not honored on API 28, so those elements are fully visible from the first scene frame instead of fading in; only the `bike` `translateX` slide actually animates. (b) The `windowBackground` shows a tiny centred mountains mark for the whole ~2 s cold start (different scale/position than the scene), so it reads as "mountains for a while, then everything snaps in". User-approved fix: **rebuild the splash as a layered Compose animation** — full control over timing/easing/staggering, fades guaranteed, no experimental AVD (no AB1 crash), and the neon glow returns (it lives in the raster, not the VD).*

***Assets are ALREADY extracted + visually verified (no inkscape needed by the loop)** — 9 full-canvas (1080×1560) transparent PNGs with glow in `app/src/main/res/drawable-nodpi/`, composited-checked to reconstruct the full hero exactly: `splash_l_mountains.png`, `splash_l_bike_body.png` (body+rider, NO wheels), `splash_l_beam.png` (headlight cone+dot), `splash_l_wheel_rear.png`, `splash_l_wheel_front.png`, `splash_l_trail.png`, `splash_l_pin.png`, `splash_l_wordmark.png`, `splash_l_tagline.png`. All share the same 1080×1560 canvas → stack with `fillMaxSize` + identical alignment and they line up. Wheel rotation pivots as canvas fractions (for `graphicsLayer` `transformOrigin`): **wheelRear = (0.1948, 0.3936)**, **wheelFront = (0.3665, 0.3965)**. Decisions: spinning wheels; calm ~2.2 s pacing. Supersedes AD1.)*

| #  | Item | Status |
|----|------|--------|
| AE1 | **Pure, unit-tested choreography seam `SplashChoreography`.** Add `ui/screens/splash/SplashChoreography.kt`: a stateless object with `const val TOTAL_MS = 2200L` and `fun stateAt(elapsedMs: Long): SplashLayers`, where `SplashLayers` is a small data class holding per-layer transform state (`alpha: Float`, `translateXFrac: Float` [fraction of width], `translateYPx: Float`, `rotationDeg: Float`, `scale: Float`) for each animated layer (bike[+beam], wheelRear, wheelFront, trail, pin, wordmark[+tagline]; mountains is constant alpha=1). Phase windows (each interpolated with a fast-out-slow-in easing, wheels linear), clamped to `[0, TOTAL_MS]`: **bike/wheels** 0–800 ms (bike `translateXFrac` −0.52→0; wheels `rotationDeg` 0→720, linear); **trail** 600–1300 ms (`alpha` 0→1 + `scale` 0.96→1); **pin** 1200–1600 ms (`alpha` 0→1 + `translateYPx` −24→0 + `scale` 0→1); **wordmark+tagline** 1500–2200 ms (`alpha` 0→1 + `scale` 0.9→1). All thresholds are named constants (easy tuning). No Android imports. **Unit tests** (`SplashChoreographyTest`): before a layer's window it is invisible/at start; inside, alpha & progress are monotonic; after, at end-state; `elapsedMs` outside `[0,TOTAL_MS]` clamps; mountains always alpha=1; `stateAt(TOTAL_MS)` = full end-state. Pure seam → `✅`-able by the loop. | ✅ |
| AE2 | **Rebuild `SplashScreen.kt`/`SplashHero.kt` as layered Compose (remove the AVD/`AndroidView`/`ImageView` path).** Replace the AVD host with a `Box(Modifier.fillMaxSize().background(Color(0xFF101114)))` stacking the AE-assets as `Image(painterResource(R.drawable.splash_l_*), contentScale = ContentScale.Fit, modifier = Modifier.fillMaxSize().graphicsLayer { … })`, z-order back→front: mountains, trail, beam, bike_body, wheel_rear, wheel_front, pin, wordmark, tagline. Drive each layer's `graphicsLayer` (`alpha`, `translationX = translateXFrac * size.width`, `translationY`, `rotationZ`, `scaleX/scaleY`) from `SplashChoreography.stateAt(elapsed)`, where `elapsed` comes from a single `Animatable` run in a `LaunchedEffect` (`animateTo(SplashChoreography.TOTAL_MS.toFloat(), tween(TOTAL_MS.toInt(), easing = LinearEasing))`; map its value → `stateAt`). Wheels: set `transformOrigin = TransformOrigin(0.1948f, 0.3936f)` (rear) / `TransformOrigin(0.3665f, 0.3965f)` (front); wheels also get the bike's `translateXFrac` (they slide in WITH the bike, then/while spinning). Keep the bottom status label (`SplashStatus.labelFor`). **Fallback:** wrap layer loading so that if any `splash_l_*` resource is missing it shows a single static `Image(painterResource(R.drawable.splash_portrait_hd))` — composition never throws. Unit-test any pure decision seam (e.g. fallback selection); the on-screen animation is `🔬`. Depends on AE1. | 🔬 |
| AE3 | **Seamless windowBackground + gate timing + cleanup.** (1) `res/drawable/splash_window_background.xml` → solid `#101114` + `splash_l_mountains.png` (`android:gravity="fill"`, full canvas) so the OS cold-start frame matches the Compose `mountains` layer exactly (no scale/position jump); keep it wired via `Theme.MotoTracker.Launch`. (2) `SplashGate` (`SplashPhase.kt`): set the `MainActivity` call site's `minDurationMs = SplashChoreography.TOTAL_MS` (2200) so the splash is never dismissed before the animation ends (fixes the current 2000-vs-2100 clip); bump `maxDurationMs` to 3000; update the stale KDoc that references `splash_hero_avd`/`AVD_DURATION_MS`. (3) **Cleanup:** delete the now-unused `res/drawable/splash_portrait_avd.xml` and `res/drawable/splash_portrait_vd.xml` (referenced only by the rewritten `SplashScreen.kt`); leave the landscape `splash_hero_*` assets untouched. Clean `assembleDebug` + `lintDebug`; on-device `🔬` (screen ON: mountains hold → bike rides in with spinning wheels → trail fades in → pin drops → wordmark+tagline fade in at bottom → app; no white flash, no snap, no crash). Depends on AE2. | 🔬 |
| AE4 | **Preload the routes list during the splash so the first screen is instantly populated.** Today only the startup DataStore decision + the Room DB open (via `syncState`'s eager `syncQueueDao.getPendingCount()`) happen during the splash; the **routes list is not loaded until `RoutesViewModel` is created on navigation**, so the ~2.2 s splash time is not used to fetch content. Make it productive: in `AppStateViewModel` (activity-scoped — created as soon as the splash composes, so this runs DURING the animation) add `val preloadedRoutes: StateFlow<List<RouteSummaryModel>> = routeRepository.observeSummaries().stateIn(viewModelScope, SharingStarted.Eagerly, emptyList())` — this runs the Room summaries query and warms the DB while the splash plays, and the result stays cached for the activity's lifetime. Then wire the routes screen so its **initial** content comes from `preloadedRoutes` (seed `RoutesViewModel`'s initial value from it, or pass it into the screen as the first-frame list) so navigating to Trasy shows the list immediately with no spinner; `RoutesViewModel`'s own `observeSummaries` flow keeps driving live updates afterwards. Do NOT block splash dismissal on this preload (it is best-effort; the gate still uses `SplashChoreography.TOTAL_MS`) and do NOT change the offline-first behavior. **Unit tests:** with a fake `RouteRepository` emitting a summaries list, `AppStateViewModel.preloadedRoutes` reflects it eagerly WITHOUT the routes screen ever being shown (proves it loads during splash); the routes screen/VM uses the preloaded list as its initial state. Clean `assembleDebug`+`lintDebug`. On-device `🔬` (Trasy tab populated instantly after splash). Depends on AE2 (splash) — independent of AE1/AE3. | 🔬 |

*(AE1 = testable timing brain; AE2 = layered Compose rendering with the ready glow PNGs [guaranteed fades, spinning wheels, no AVD]; AE3 = seamless cold-start handoff + correct dismiss timing + remove dead AVD/VD; AE4 = make the ~2.2 s productive by preloading the routes list in the activity-scoped `AppStateViewModel` during the splash so the landing screen is pre-populated. Root cause of AD1's failure: `<group android:alpha>` animation is a no-op on API 28 — hence the move off AVD to Compose graphicsLayer alpha, which always works.)*

## AF. Splash animation timing — kill the cold-start "jump" (on-device diagnosis 2026-07-20, round 26)

*(On-device (P20, Android 9, debug build) frame-by-frame + timed capture of the AE layered splash found the choreography brain (`SplashChoreography`, TOTAL_MS=2200) and `MainActivity` gate wiring are CORRECT, but the animation does not read as the intended slow staged reveal. Two runtime problems, both from heavy cold-start work on the main thread: (1) **long static-mountains hold** (~several seconds — measured up to ~8 s on a cold debug launch) before the first Compose frame draws, because app init [Hilt + eager `AppStateViewModel` flows incl. the AE4 route preload] + decoding NINE full-screen 1080×1560 PNG layers happens before first paint; (2) **the animation "jumps"** — Compose's `tween`/`Animatable` is wall-clock/frame-delta driven, so when it finally starts, the FIRST rendered frame carries a huge frame delta (main thread busy decoding the 9 bitmaps) and the animation skips most of its 2.2 s timeline on that first frame → the trail→pin→wordmark stagger collapses into ~1 visible frame. Fades technically work (an intermediate frame shows bike mid-entrance with the trail dim and no pin/wordmark) but are imperceptible. Fix: decouple the animation clock from cold-start jank. Device-dependent [worst case = old P20 + debug]; AF1's first-delta clamp removes the jump on ANY device. Follows AE.)*

| #  | Item | Status |
|----|------|--------|
| AF1 | **Drive the splash clock with a frame-delta-clamped manual driver so the staged reveal always plays on real frames (no jump).** In `SplashScreen.kt`'s `LayeredHero`, replace the `Animatable(0f).animateTo(tween(TOTAL_MS, LinearEasing))` with a manual frame loop that accumulates elapsed time from clamped per-frame deltas: `var elapsed by remember { mutableLongStateOf(0L) }`; in `LaunchedEffect(Unit)` do `var last = withFrameMillis { it }; while (elapsed < SplashChoreography.TOTAL_MS) { withFrameMillis { now -> val d = (now - last).coerceIn(0, 32); elapsed = (elapsed + d).coerceAtMost(SplashChoreography.TOTAL_MS); last = now } }` (t=0 is the FIRST actually-rendered frame; each frame advances by at most ~32 ms so a janky first frame can no longer skip the timeline). Feed `elapsed` to `SplashChoreography.stateAt(elapsed)`. **Keep the gate honest:** the splash must stay visible until the manual `elapsed` reaches `TOTAL_MS` (the animation genuinely finished on-screen), not just until wall-clock min elapses — expose the animation-complete signal (e.g. hoist `elapsed`/an `onAnimationComplete` callback, or have `MainActivity` gate also require the frame-driven animation done). Unit-test any pure seam added (e.g. a `clampDelta(prev,now,max)` helper); the smooth on-screen playback is `🔬`. Independent of AF2/AF3. | 🔬 |
| AF2 | **Stop starving the main thread during the splash: preload the 9 layer bitmaps off the critical path and move the AE4 route preload off the startup main dispatcher.** (1) The AE4 `preloadedRoutes` eager `stateIn` in `AppStateViewModel` launches on `viewModelScope` (Dispatchers.Main.immediate) during ViewModel init — ensure the Room work runs on IO (the Room `observeSummaries` query already uses Room's executor, but confirm no main-thread blocking; if the `combine`/mapping does any work, `.flowOn(Dispatchers.IO)` it) so it cannot delay the first splash frame. (2) Decode the nine `splash_l_*` PNGs so the first animated frame is not competing with bitmap decode: e.g. warm them via `ImageBitmap`/`painterResource` in a `LaunchedEffect` before flipping to the animated state, or ensure they are loaded once and cached. Do NOT change the visual result. Unit-test the dispatcher/threading seam if extracted; on-device `🔬` (shorter static-mountains hold before the animation starts). Independent of AF1/AF3. | 🔬 |
| AF3 | **Trim the dead static periods around the animation.** After AF1, the splash should start animating on the first frame and dismiss right after the animation completes (+ a short hold), with no long static-full-scene tail. Verify `MainActivity`'s gate: `maxDurationMs` should not force a long static hold, and once the frame-driven animation completes AND `isReady`, dismiss promptly (small settle, e.g. ≤200 ms). Confirm the `windowBackground` mountains still match the Compose `mountains` layer position (no jump at handoff). Clean `assembleDebug` + `lintDebug`. On-device `🔬` (screen ON: brief mountains → bike rides in with spinning wheels → trail fades in → pin drops → wordmark+tagline fade in → app; the four phases are each perceptible, no jump, no long dead holds). Depends on AF1. | 🔬 |

*(AF1 = frame-delta-clamped manual clock so the reveal can never skip [device-independent core fix]; AF2 = keep the main thread free during the splash so it starts sooner and doesn't jank; AF3 = remove the static dead time bracketing the animation. Root cause: Compose wall-clock tween + heavy cold-start main-thread work = the 2.2 s staged reveal collapses into one frame.)*

## AG. Splash wheels must spin on their own axis, not orbit (on-device 2026-07-21, round 27)

*(On-device (P20, 1080×2280) the AF splash plays the staged reveal correctly, but the spinning wheels **wobble/orbit** instead of rotating about their hubs. Root cause: each `SplashLayer` is `Modifier.fillMaxSize()` (the full-screen composable = 1080×2280) with `Image(contentScale = ContentScale.Fit)`; the 1080×1560 layer artwork is therefore letterboxed — scaled to 1080 wide and centred vertically, occupying screen-y ≈ 360..1920 with ~360 px dark bars top/bottom. But `graphicsLayer.transformOrigin` is a fraction of the **layer bounds** (the full 2280-tall composable), so the wheel pivots — `wheelRear (0.1948, 0.3936)` / `wheelFront (0.3665, 0.3965)`, which are fractions of the **1080×1560 artwork** — land ~77 px ABOVE the actual hubs on screen. Rotating about a point above the hub = the wheel swings/orbits. NOTE: the "long mountains before the animation" is NOT a real bug — it is a debug-build artifact: measured cold-start time-to-first-frame is 7.6 s on debug vs **1.5 s on release** (`am start -W`); on release the mountains show ~1.3 s then the ~2.2 s animation plays. Decision: do NOT optimize cold start; only fix the wheel pivot. Follows AF.)*

| #  | Item | Status |
|----|------|--------|
| AG1 | **Fix the wheel rotation pivot by constraining the layer stack to the artwork's exact aspect ratio (no letterbox mismatch).** In `SplashScreen.kt`'s `LayeredHero`, wrap the nine `SplashLayer`s in an inner container sized to the artwork aspect ratio and centred in the full-screen splash `Box`: e.g. `Box(Modifier.fillMaxWidth().aspectRatio(1080f / 1560f).align(Alignment.Center)) { …layers… }`, and change each `SplashLayer`'s modifier from `fillMaxSize()` to fill THIS container (still `fillMaxSize()` inside the aspect-constrained box). Because the container now has exactly the 1080:1560 artwork ratio, `ContentScale.Fit` fills it with no internal letterbox, so `transformOrigin` fractions map 1:1 onto the artwork and the wheel pivots land exactly on the hubs (rear `(0.1948, 0.3936)`, front `(0.3665, 0.3965)`) → wheels spin about their own axis. Also confirm `translationX = state.translateXFrac * size.width` now uses the container width (consistent with the artwork), and that the scene's on-screen position is unchanged (the aspect box centred in a 2280-tall screen occupies the same ~360..1920 band the Fit letterbox produced, so mountains still line up with the `windowBackground`). Keep `contentScale = ContentScale.Fit` on the images (a belt-and-braces; with the matched aspect it is effectively 1:1). Do NOT change `SplashChoreography` or timing. Unit-test any pure seam if added (none likely needed — this is a layout/pivot fix); on-device `🔬` (screen ON, release build for realistic timing: bike rides in and both wheels visibly rotate about their hubs, not orbiting). Independent; supersedes the pivot wiring from AE2. | 🔬 |

*(AG1 = wheels spin on-axis by removing the Fit-letterbox mismatch between the transformOrigin's reference frame [full-screen layer] and the artwork [1080×1560]. The "long mountains" complaint is a debug-only cold-start artifact [7.6 s debug vs 1.5 s release] — intentionally NOT addressed.)*

## AH. Connect the app to the real GPStrack backend: routes visible in web + account registration (design 2026-07-21, round 28)

*(App↔GPStrack integration, sub-project 2 of 3 [backend done → APP (this) → web]. **The backend API is LIVE and verified** on the server (`192.168.1.145`): endpoints are real `.php` files — `register.php`, `login.php`, `api_routes.php` — auth by PHP session cookie. Two problems on the app side: (1) `HttpGpStrackClient` points at endpoints that DON'T EXIST on this backend — `ENDPOINT_LOGIN = "/login"` and `ENDPOINT_ROUTES = "/api/routes"` (no `.htaccess`/rewrite on the server) — so login and the whole route-upload path silently 404; the sync queue IS otherwise fully wired (RecordingViewModel enqueues on save; SyncRepositoryImpl auto-drains on network+settings → `uploadRoute`), it just hits a dead URL. (2) There is NO account registration in the app (only `LoginScreen`/`LoginViewModel`). Fix both so routes recorded in the app actually appear in the web GPStrack, and users can create an account from the app. Backend contract [verified]: `register.php` POST JSON `{email,password,display_name?}` → 200 `{ok,user_id}` + Set-Cookie (auto-login), 409 `email_taken`, 400 `invalid_input` (`detail: invalid_email|weak_password`, password min 8); `login.php` accepts `email` (JSON or form) → 200 + Set-Cookie; `api_routes.php` POST JSON of the Route, session-authed, upsert by `id` (=client_uuid) → 200 `{ok,route_id}`, 401 when no session. Batch upload after the ride [existing sync behavior]. Full contract: `docs/superpowers/specs/2026-07-21-gpstrack-backend-api-design.md`.)*

| #  | Item | Status |
|----|------|--------|
| AH1 | **Point the network client at the real backend endpoints so login + route upload actually work.** In `data/network/HttpGpStrackClient.kt` change the endpoint constants to the real server files: `ENDPOINT_LOGIN = "/login.php"` and `ENDPOINT_ROUTES = "/api_routes.php"` (the backend has no URL rewriting, so the current `/login` and `/api/routes` 404). No other logic changes needed: `login()` already POSTs `email=…&password=…` form-encoded (backend `login.php` accepts the `email` field), and `uploadRoute()` already POSTs the Route JSON with the session `Cookie` and clears the session + throws `UnauthorizedException` on 401 (backend returns 401 without a session). The route JSON already includes `id` (the stable local route id → the backend's `client_uuid` upsert key), `name`, `dateEpochMs` (the 3 fields `api_routes.php` requires) plus `km`/`durSec`/`avg`/`max`/`pathJson`/… **Unit test** (`HttpGpStrackClientTest` with the existing `FakeHttpTransport`): assert `login()` requests a URL ending `/login.php` and `uploadRoute()` a URL ending `/api_routes.php`; existing success/401/cookie assertions still pass. On-device wiring (real upload → row in web) is `🔬`. Small, unblocks the whole integration. | ⬜ |
| AH2 | **Add "Create account" (registration) in the app, wired to `register.php` with auto-login.** (1) **Client:** add `suspend fun register(serverAddress: String, email: String, password: String): Result<Unit>` to `GpStrackClient` + implement in `HttpGpStrackClient`: POST `application/json` `{"email":…,"password":…}` to `<serverAddress>/register.php`; on HTTP 200 parse the `Set-Cookie` (reuse `parseSessionCookie`) and persist via `sessionStore.save(cookie, email)` (auto-login, same as `login()`); map **409 → a distinct failure** (e.g. `EmailTakenException`) and 400 → invalid-input failure so the UI can message them; 2xx-without-cookie still succeeds (session optional). (2) **UI:** add a `RegisterScreen` + `RegisterViewModel` (mirror `login/` package: `RegisterUiState` with serverAddress/email/password[/confirm], `RegisterEvent`), reusing `LoginScreen`'s field styling/colors. Client-side validation matching the backend: valid e-mail + password **min 8 chars** (+ optional confirm-password match) before enabling the button; on submit call `register`, on success route into the app (same post-auth destination as sign-in), on `EmailTakenException` show "e-mail już zajęty", on invalid-input show a field error. (3) **Navigation:** add a "Załóż konto" affordance on `LoginScreen` that navigates to `RegisterScreen`, and a back-to-login link; wire the route into `ui/navigation` (MotoApp). (4) **Strings:** every new user-facing string as a resource in **all 6 languages (pl/en/de/fr/cs/ru)** — screen title, labels, button, validation/error messages. **Unit tests:** `HttpGpStrackClient.register` (fake transport) for 200+cookie→success, 409→`EmailTakenException`, 400→invalid-input, 2xx-no-cookie→success; `RegisterViewModel` validation seam (invalid email / short password disable submit; success + error-mapping transitions). Clean `assembleDebug`+`lintDebug`. On-device (real account creation on the server) is `🔬`. Depends on AH1. | ⬜ |

*(AH1 = one-line-ish endpoint fix that makes the existing login + already-wired sync queue actually reach the live backend [routes start landing in the web DB]; AH2 = new registration screen/flow + client method with auto-login, so a user can make an account from the app. Web view of those routes = sub-project 3, separate. Backend endpoints are already live + verified on 192.168.1.145.)*
