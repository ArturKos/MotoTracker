package com.mototracker

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Sanity-check unit test verifying that the JUnit test runner is wired up
 * correctly after the initial project scaffold.
 */
class ExampleUnitTest {

    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
